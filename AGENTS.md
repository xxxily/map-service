# 项目协作规则

## 工作方式

- 本项目按前后端分离协作开发：后端先交付稳定接口、数据契约、测试和文档，前端基于文档和真实接口对接。
- 跨端能力必须先明确接口契约，包括路径、方法、鉴权、请求体、响应体、错误码、状态流转和边界条件。
- 后端交付不得只停留在代码实现；必须同步提供自动化测试、接口文档和必要的开发记录。
- 重要开发进展、决策和未完成事项应记录到 `docs/changelog.md`、相关需求文档或专门的开发文档中，避免只存在于对话里。

## 文档

- 项目文档统一使用中文。
- 较大的产品或系统改动，应先更新 `docs/requirements/` 下的需求文档。
- 新增或调整后端接口时，必须同步更新 `docs/api.md` 或对应专题文档。
- 需求、接口和实现不一致时，以最新需求文档和接口文档为准；发现冲突应先修正文档再继续实现。
- 废弃旧能力时，应在原文档顶部标明状态和替代方案，不直接删除历史背景。

## 后端开发

- 后端接口统一归入 `/api/v1`，管理接口归入 `/api/v1/admin`，公开接口必须明确安全边界。
- 服务层应承载业务规则，路由层只做鉴权、参数读取、调用服务和响应封装，避免把业务逻辑堆在 `simpleApi.js`。
- 持久化运行态数据优先复用 `service/bin/admin/store.js`，避免散落写文件。
- API 返回结构应保持项目现有 `jsonSuc` / `jsonErr` 风格，错误消息使用中文且对外不泄露敏感细节。
- 新增数据模型必须有 normalize/validate/sanitize 流程，公开接口不得返回密码、Token 明文、代理认证信息或敏感请求头。
- 对前端开放的 catalog/配置接口只返回渲染和交互所需字段，不返回管理备注、密钥、代理账号密码等服务端内部字段。
- 后端应优先实现可测试的纯函数和服务方法，再接入路由。

## 前后端契约

- 后端给前端的交付物至少包括：接口路径、方法、鉴权方式、请求示例、响应示例、错误示例和字段说明。
- 枚举值、状态值、ID 规则、分页/排序/过滤参数必须在文档中写清楚。
- 前端未开始前，后端不得依赖前端本地状态或页面假设来补齐关键业务规则。
- URL 中用于分享或引用的标识必须是稳定 ID，不使用中文显示名作为主键。

## 前端交互

- 禁止在业务代码中直接使用浏览器原生阻塞弹窗：`alert`、`confirm`、`prompt`，包括 `window.alert`、`window.confirm`、`window.prompt`。
- 需要提示、确认或输入时，必须使用项目内统一的 Web 组件。
- 当前统一弹窗组件位于 `src/ui/dialog.js`，新增交互应复用它或在同一组件体系内扩展。

## 安全约束

- 禁止新增任意 URL 代理入口；对外瓦片请求必须通过受控图源 ID 或发布项 ID 解析。
- URL 模板、代理出口、请求头、Token、访问控制和缓存清理都必须做服务端校验。
- 代理账号密码、Token、敏感请求头只允许服务端保存，日志和 API 响应必须脱敏。
- 涉及 SSRF 风险的能力必须禁止访问内网地址、localhost、link-local、metadata 服务和非预期协议，除非需求文档明确允许并有独立保护。
- 管理接口除登录和明确公开的健康检查外必须鉴权。

## 测试与质量

- 新增后端服务、路由、模型校验、鉴权、安全边界和错误分支必须补充 `node:test` 自动化测试。
- 测试应覆盖成功路径、非法输入、鉴权失败、敏感字段脱敏、禁用状态和关键边界条件。
- 修复 bug 时应先补能失败的测试，或在提交说明中明确无法自动化的原因。
- 后端改动至少运行相关测试；提交前按项目要求运行 `npm run check`、`npm test`、`npm run build`。
- 如果因环境原因无法运行完整校验，必须在最终说明中写明已运行项、未运行项和原因。

## 工程约定

- 依赖管理只使用 npm，不重新引入 Yarn。
- 不手动编辑 `service/app/` 下的构建产物；修改前端源码后通过 `npm run build` 生成。
- 提交前至少运行 `npm run check`、`npm test`、`npm run build`。
- 手工代码编辑优先使用 `apply_patch`。
- 不回退未确认来源的已有改动；遇到无关脏文件只忽略，遇到冲突先理解再处理。
- 运行命令遵循本地 RTK 约定，优先使用 `rtk <command>`。
