# 变更日志

## 1.5.50 - 2026-08-27

### KML 移动端界面修复

- 地图 KML 面板和账号页目录标题统一为网格布局，目录名称、数量徽标、显隐按钮和箭头使用同一垂直中心；长名称省略且不覆盖按钮。
- 回收站的“返回使用中 KML”缩短为“返回”，完整含义保留在悬停标题。
- KML 文件卡片继续默认只显示显隐和更多两个按钮，新建目录保持纯图标入口。

### 管理员配置自由裁量与运输层保护

- 移除会话、KML 数量/要素配额、分享文件数、授权时长、强制聚合点数、限流和空间策略的旧固定业务最大值；管理员可按部署能力设置大于旧上限的值。
- 保留缩放级别 `0～24`、用户总要素数不得小于单文件要素数、不限授权阈值不得超过总体阈值等真实边界。
- 单个分享不再固定最多 20 个 KML；公开配置下发 `share.maxFilesPerShare`，账号端选择和服务端分享校验统一使用管理员值。
- 新增部署级 KML 运输层限制：multipart 默认 `50 MiB`，KML JSON 默认取 `64 MiB` 与 multipart 上限 `1.25` 倍中的较大值，分别支持 `MAP_SERVICE_KML_IMPORT_MAX_BYTES` 和 `MAP_SERVICE_KML_JSON_MAX_BYTES`；超限返回明确的 `413` 错误码。
- 管理设置和用户列表返回脱敏 `technicalLimits`，后台只为技术硬边界渲染动态 `max`，个人配额覆盖使用同一运输层及关系校验。
- 历史系统/个人配额在读取时忽略未知或非法字段，并安全收敛超运输层文件大小及冲突的要素关系；仅修改用户资料不再被旧配额阻断。

### 验证

- 专项覆盖超过旧业务上限的设置、超过 20 个 KML 的分享、个人配额关系、multipart 413、目录布局和返回文案。
- Playwright 在 `320/375/390px` 下测得地图目录名称/数量/操作区中心线差为 `0px`、横向溢出为 `0`；账号页同结构探针中心线差为 `0px`。

## 1.5.49 - 2026-08-27

### KML 回收站、按需加载与批量能力

- “我的 KML”新增回收站入口、恢复能力和永久删除密码二次校验；同步接口拒绝永久删除指令，避免绕过高风险确认。
- 隐藏的个人 KML 和分享 KML 首屏只加载摘要，显示、展开、导出、编辑、分享或批量操作时按需加载详情；并发请求合并，失败保留可重试状态，未加载文件不会以空要素覆盖服务端内容。
- 目录支持配置控制的批量下载和批量删除选择模式；KML 要素批量移动、复制、删除限定在单个文件内，减少跨文件误操作。
- 修复移动端目录名称、数量和操作图标错位，文件卡片继续保持“显隐 + 更多”紧凑布局。

### 聚合与性能

- 缩小 2D/3D 分享点位聚合标记，新增管理员强制聚合开关、最大缩放级别和最少点位阈值，默认仍沿用分享原有显示效果。
- 增加 20 万点、5,000 混合点线面和 100 文件容量基准；隐藏文件懒加载、请求合并和渲染批处理不裁剪要素，也不改变 Leaflet 连续缩放动画。

## 1.5.48 - 2026-08-26

### KML 恢复审计与并发一致性修复

- KML 从回收站恢复现在在同一事务内写入 `kml.restore` 审计记录，保留恢复前 revision 和要素数量摘要。
- 恢复采用 `status = 'trashed'` 条件状态转换并检查实际影响行数；并发恢复、删除或永久删除已改变状态时返回 `409 KML_REVISION_CONFLICT`，不递增错误 revision、不写入伪造审计。
- 增加恢复审计、竞态状态转换和无副作用失败回归测试，并同步更新 API/需求契约。

## 1.5.47 - 2026-08-26

### KML 账号同步数据安全修复

- 修复管理后台与地图切换、分页/详情加载不完整或目录竞态时，将暂时缺失的 KML 误判为批量删除的问题；工作集只在完整加载后替换，服务端 ID、创建幂等 ID 和本地工作 ID 分离。
- `trash` 改为显式用户意图，服务端逐条拒绝无 `user-confirmed` / `user-confirmed-batch` 的回收站操作；旧草稿中的无确认删除墓碑会被忽略并补回 active 文件。
- 保存失败保留恢复草稿和最新 pending；严格校验同步响应，畸形 2xx 不会清空待保存内容；在途失败期间的新编辑按最新工作集重建，不自动无限重试。
- 认证刷新失败时保留已有数据用于查看但强制只读；冲突自动合并重试有上限，避免单端操作也频繁出现冲突和保存失败。
- 新增《账号 KML 同步数据丢失防护》需求与接口契约，并补充旧草稿、响应完整性、在途编辑、认证失败和删除确认回归测试。

## 1.5.46 - 2026-08-26

### KML 目录管理、文件排序与分享点位聚合

- 完成个人 KML 单层目录模型与 SQLite v9 迁移，支持目录新增、重命名、排序、批量显隐和删除后转入“未分类”；文件支持目录归属、同目录排序、跨目录移动及选择式移动，文件拖拽协议与点位/线段要素拖拽协议保持隔离。
- 统一账号页、地图 KML 面板和“我的分享”的目录/文件顺序、稳定 ID、显隐状态和分享选择；目录分享在发布时展开为文件快照，历史无目录字段的分享继续兼容，目录改名或移动不会隐式改写已发布快照。
- 整理 2D/3D KML 数据管理面板：目录、文件卡片、操作按钮和设置区使用统一分栏与换行规则，长名称自动省略，移动端面板保持完整滚动区域，空目录和显隐状态清晰展示。
- 分享配置新增可选 KML 点位聚合，默认关闭且只作用于公开分享 2D Point；开启后按缩放级别和像素网格递归聚合，聚合图标显示准确成员数并支持逐级放大，LineString/Polygon 和 3D Cesium 首阶段保持原渲染路径。
- 补充目录、排序、移动、分享快照、聚合配置、账号同步和 UI 回归测试；`rtk npm test` 结果为 936/936 通过，`rtk npm run check` 和 `rtk npm run build` 均通过。构建仅保留既有大包体积提示。
- 修复账号 KML 保存冲突：服务端规范化后的 canonical 内容会安全回写，保留请求在途期间的新编辑；延迟旧响应不再回退 revision，避免重复保存和 409 冲突。
- 优化 2D/3D KML 文件卡片，默认只显示显隐和更多两个图标；重命名、移动、分享、导出等操作在更多操作行展开，文件名获得稳定显示空间；新建目录统一为 folder-plus 图标按钮。

### Artalk 可选依赖边界说明

- 统一明确内部 Interaction Service 独立承载留言、审核、举报和 AI 辅助审核；Artalk 仅为可选单向镜像和第三方 provider POC，未安装、关闭或故障不会阻塞交互主链路。
- 补充代码默认关闭、161 内测显式开启、66 生产强制关闭的环境差异，以及 Artalk 后台不作为正式审核工作台、手工修改可能被内部状态校准覆盖的边界。
- 说明 `/admin/interaction-ai` 当前只提供 Artalk 脱敏状态、连接验证和排空校准，不提供 Endpoint 或凭据编辑；161 专用发布脚本要求 Artalk 是内测验收策略，不代表应用运行依赖。

## 1.5.45 - 2026-08-25

### Artalk 修复完整发布

- 将 `v1.5.44` 的 Artalk `403 AdminGuard` 鉴权修复与注入应用版本号的前端静态构建产物同步发布，确保 Git 标签、页面版本和 161 运行镜像一致。

## 1.5.44 - 2026-08-25

### Artalk 161 镜像鉴权修复

- 修复 Artalk `AdminGuard` 对评论更新/删除返回 `403` 时未触发管理员登录重试的问题；服务端镜像适配器现在对 `401/403` 最多刷新一次 Bearer Token，登录后仍失败则 fail-closed，不会无限重试。
- 新增 Artalk 403 鉴权重试与二次失败停止的回归测试，确保 `pending_default` 场景下内部已批准留言可以安全投影为公开镜像。

## 1.5.43 - 2026-08-25

### Phase 3 运营闭环与 Artalk 161 受控实测

- 后台“AI 审核与规则”补齐规则试运行、AI 策略影响预览、提示词版本发布、失败审核事件重放和单条留言 AI 重放；配套 RBAC、CSRF、服务层与 API 集成测试。
- AI 活动策略发布后立即同步运行时引擎；只影响 provider、提示词/策略版本、超时重试、预算、并发和等级动作，不改变留言、匿名留言或举报总开关。
- Artalk `2.10.0` 以固定镜像 digest 部署到 161 内网的隔离 sidecar，完成待审不公开、管理员审核/删除、Artrans 导出、临时 SQLite 导入、重启持久化与可信 Origin/CORS 验收。
- 修正 Artalk 1Panel 模板元数据、参数化 Compose 和官方图标；管理凭据与 app key 已轮换，仅保留在 161 受限运维文件。
- 161 一键发布脚本会校验并读取受限 Artalk 凭据、备份 `.env` 与 SQLite 快照后自动启用镜像；66 一键发布脚本强制保持 Artalk 镜像关闭。
- Artalk 仅用于 161 适配器与迁移实测；公开页面仍通过同源 Interaction Adapter 访问内部 Comment/Moderation Service，禁止浏览器绕过分享授权和最终审核状态。

## 1.5.42 - 2026-08-24

### 媒体详情配置与交互体验优化

- 留言隐私同意项在需要同意时默认勾选；后台新增可扩展的 `mediaDetails.generalDescription` 全局配置，并通过公开 info 接口安全下发。
- 重新设计媒体详情对话框的摘要、来源说明、媒体说明和事实信息布局，详情按钮和留言按钮保持可用，举报入口仅位于详情面板底部。
- 161、66 发布脚本改为本机运维资产；`deploy-161.sh` 和 `deploy-66.sh` 均加入忽略规则，不会随 GitHub 代码发布。

## 1.5.41 - 2026-08-24

### 161 一键部署脚本稳定性修复

- 修复 `deploy-161.sh` 在 `set -u` 下退出清理阶段引用函数局部归档变量导致的非零退出；发布成功现在会稳定返回 0，并继续清理本地临时发布包。

## 1.5.40 - 2026-08-24

### 留言、媒体详情与举报交互修复

- 管理后台新增“留言与举报设置”，可直接配置留言总开关、匿名留言、联系方式要求、同意确认、审核要求和匿名举报；保存时保留未展示的审核、AI、关键词、保留期及目标范围字段。
- 修复媒体预览中留言和来源详情按钮被同一资源条件联动禁用的问题；普通分享媒体和资源集合媒体会携带稳定的分享项、Feature 与 Media 引用。
- 举报入口从媒体预览顶部移入媒体详情面板，继续复用统一举报表单和同意确认流程。
- 匿名留言表单按服务端策略显示显示名、邮箱/手机号和同意字段，并携带幂等请求 ID。
- 新增 `deploy-161.sh` 一键发布/回滚脚本：发布前执行检查、备份代码与 1Panel 模板，保留 `.env`、管理员密码、数据库和日志目录，部署后执行双健康检查。

## 1.5.39 - 2026-08-24

### 举报治理动作修复

- 修复新举报直接执行暂停分享、封禁分享等治理动作时，被举报状态机错误拒绝并返回 `MODERATION_TRANSITION_INVALID` 的问题。
- 补充 `new -> actioned` 与 `new -> investigating` 状态转换测试，并覆盖 `pause_share`、`request_more_info` 的服务层回归场景。

## 1.5.38 - 2026-08-24

### 留言、审核与举报闭环

- 发布 Phase 1A-F 交互能力：独立 SQLite 数据库、留言提交与可见性、关键词/AI/人工审核、举报工单、分享治理动作、RBAC/CSRF 防护、保留清理、备份恢复和运维指标。
- 新增中文部署与接入手册，覆盖初始化、公开/管理 API、配置、升级回滚、备份和 agent 执行协议；Artalk 生产接入继续延期，AI provider 默认关闭。
- 合并 v1.5.37 分享生命周期修复：发布快照、暂停/封禁/删除及访问数据清理契约保持兼容。

### Phase 3 AI 审核与最终质量门

- 新增[交互功能部署与接入手册](./interaction-deployment-and-integration.md)，覆盖首次部署、策略初始化、公开/管理 API 接入、Cookie/CSRF、AI provider 验证、保留任务、备份恢复、升级回滚、agent 执行协议和故障排查；明确单机单写进程边界以及 Artalk 生产部署延期。
- 新增服务端受控 AI provider adapter factory（当前 `openai-compatible`）与健康验证 API；管理 API 只保存声明式 adapterId/endpoint/secretRef，不能提交任意函数。provider 未通过健康验证时保持不可用且不能设为默认。
- provider endpoint 强制非空 allowlist，实际请求复用 DNS 解析、公开地址固定和重定向拒绝；provider 级并发、每日预算、重试、超时和熔断配置进入执行面，并持久化每日用量。
- 明确 provider 运维契约：`dailyBudget=0` 表示无限制；健康验证默认 TTL 为 24 小时，修改 endpoint、密钥引用、adapter、模型、提示词、超时、重试、预算、并发或脱敏配置后自动失效并必须重新验证。
- 新增异步 AI 审核引擎：严格结构化 JSON、提示词/策略版本、超时、坏 JSON、低置信度、`unknown`/`illegal_or_ip` fail-closed；AI 只追加审计建议，不改变留言最终审核状态，人工审核保持最终权威。
- AI 外发正文执行邮箱、手机号、IP、会话令牌和内部 ID 的内容级脱敏；留言和匿名联系方式写入独立保留到期字段，运维清理先擦除联系方式再删除过期留言。
- AI 原始 provider JSON 仅在 64KB 内保存为交互密文，默认 30 天到期；管理详情只返回脱敏分数、置信度、结果哈希和保留期状态。关闭服务会等待在途 AI 审核完成，避免半写入审计。
- 为已有 interaction v1 数据库增加幂等 additive AI schema 迁移，并清理旧 `raw_result_encrypted` 字段中的交互密文前缀数据；新增旧库重开、provider 持久化、人工覆盖和生命周期回归测试。
- `npm run check` 的静态检查现在覆盖 `shared/interaction-ai.js`、`providerRegistry.js` 和 `aiModeration.js`。
- 统一交互保留配置入口为 `config.staticService.interaction`，补充 report/report-event/outbox 窗口环境变量；retention cron 每日 Asia/Shanghai 03:20 执行，并增加真实配置路径回归测试。

### Phase 1B/C 留言、审核与举报服务/API

- 接入 Interaction Adapter、Comment Service、Moderation Service 和交互服务 facade；公开 `publicId` 经现有分享授权解析为 canonical share，并按已发布快照校验 `shareItemId + featureId`，资源不存在或无权访问时防枚举失败关闭。
- 新增公开留言策略、列表、准确计数和提交接口；公开列表/角标只包含 `active + approved`，登录写入校验 CSRF，匿名写入同时受策略开关和同源校验约束，提交统一返回 `202` 审核回执且不回显待审正文。
- 新增管理端留言列表、详情、人工审核、按当前策略重新审核、软删除、策略发布和关键词规则发布接口；重新审核保留历史决策，并按留言内容修订与策略/关键词版本幂等；所有管理写入使用细分 RBAC、CSRF、`no-store` 和审计记录，响应不返回联系方式、密文、内部用户 ID、Token 或 IP/UA。
- 建立确定性关键词版本、人工决策和 outbox 重试/幂等处理链路；interaction 定向测试 91/91、API 集成测试 4/4，`npm run check` 通过。
- 接入独立 Report Service、公开举报提交和举报管理列表/详情/动作 API；支持分享/点位/媒体目标、匿名/登录提交、侵权权利声明、幂等与时间窗口重复合并、加密正文/联系方式和脱敏管理视图。
- 举报正文不进入公开留言、关键词或 AI 审核流；分享封禁/暂停动作复用现有治理并写审计，尚未接入的媒体/留言隐藏动作明确拒绝而不伪造成功。公开来源信息 `info` facade 已接入，仅返回渲染所需来源、协议和举报能力描述。
- 新增公开 `GET /api/v1/public/kml-shares/:publicId/info`，复用分享访问授权并仅返回渲染所需来源、协议和举报能力描述。

### Phase 1A 交互领域契约与独立数据库

- 新增独立 `interaction.sqlite` v1 迁移模块，不修改 UserDatabase 版本；迁移使用事务、支持重复执行，并在失败时回滚版本记录和已建表。
- 新增 `comments`、审核决策、outbox、策略/关键词版本、`reports` 和举报事件表，补齐状态约束、资源索引、幂等索引、软删除、`orphaned`、法律保留和保留期字段；留言和举报必须引用真实存在的同意策略版本。
- 回复只允许关联同资源、已批准的一级父留言；已有回复的父留言禁止变更资源身份或层级，父留言失去公开资格时回复同步隐藏，父留言孤立时回复同步孤立且不会自动恢复。
- 新增安全文本、联系方式、游标、幂等键和资源范围规范化；联系方式、留言/举报受限原文使用独立 AES-256-GCM 密文，数据库严格校验密文分段和长度，联系哈希使用 HMAC，公开留言序列化默认移除 PII 与审核内部字段。
- 解密缺少密钥、格式错误和认证失败使用不同的内部错误码并 fail-closed；Phase 1A 当时未注册公开或管理路由，现已由后续 Phase 1B/C 留言垂直切片接入。
- 补充 Phase 1A 定向测试，覆盖输入边界、密文伪造、认证加密失败关闭、回复不变量、策略版本外键、公开脱敏、迁移幂等与回滚；验证结果为定向测试 28/28、全量测试 778/778，`npm run check` 和 `npm run build` 通过。

### KML 分享生命周期与删除契约细化

- 明确个人 KML 编辑只更新源内容，外部分享继续读取最近一次成功发布的快照；只有用户显式“同步内容”才更新链接内容和空间范围。
- 明确几何重算失败、源内容修改、公开读取异常和暂时没有可计算几何不得自动暂停分享；恢复必须复用原分享设置、分享项和已发布快照。
- 规划用户与管理员删除分享接口。删除只清理分享及其访问会话、访问事件和运行指标，不删除原始 KML；撤销仍保留为可审计的不可恢复停用。
- 同步更新需求、API 和用户指南，补充 `SHARE_EMPTY`、删除权限、级联清理和旧 `publicId` 失效契约。

### KML 点位留言与举报需求规划

- 新增 [KML 点位留言、内容举报与审核治理需求](./requirements/kml-comments-and-reports.md)，明确留言、关键词过滤、AI 分级审核、人工复核、匿名 PII、举报工单、权限、API 契约和分阶段服务拆分方案。
- 需求将留言服务、审核服务、举报服务与地图集成适配层分开，第三方评论系统仅作为通过适配器 POC 后的候选实现。
- 完成 Phase 0 契约冻结：新增留言/审核/举报状态机、动作矩阵、权限目录、匿名联系方式与保留策略；公开分享快照接入稳定 Feature/Media `resourceRefs` 校验，兼容历史随机 `publicId` 和用户 Feature ID。
- 补充重复媒体来源的 occurrence 级稳定 `mediaId`，并对部分/未知资源元数据、损坏快照和缺失 `features` 执行 fail-closed 校验；完全缺少资源索引的历史快照仍只读兼容派生且不回写。
- 新增 Remark42/Artalk/Isso/Cusdis 官方资料评审与适配器 POC，Artalk 为第三方 provider POC 首选、Remark42 为第二候选，Cusdis 因归档/弃用/GPL-3.0 排除；POC 不接入生产依赖。

## 1.5.37 - 2026-08-23

### KML 分享发布与生命周期修复

- KML 编辑只更新个人源内容，已分享链接继续读取最近一次成功发布的快照；只有所有者显式执行“同步内容”才发布最新内容并重新计算范围。
- 源内容修改、几何重算失败、公开读取异常或暂时没有可计算几何时不再自动暂停分享；暂停、恢复沿用原分享设置和已发布快照。
- 新增用户和管理员删除分享能力，删除级联清理分享项、访问会话、访问记录和运行指标，保留原始 KML，旧 `publicId` 立即失效；撤销仍保留为可审计停用。
- 同步更新需求、API 契约、用户指南和前后端回归测试。

## 1.5.36 - 2026-08-22

### 分享地图旋转拖拽稳定性

- 修复空间受限 KML 分享多次旋转、缩放或调整窗口尺寸后继续沿用旧视口快照，导致部分 KML 内容无法拖入查看的问题；每次状态变化后按当前 bearing、zoom、视口尺寸和投影范围重新计算。
- 边缘拖动改为保留阻尼位移并交由 Leaflet 原生边界动画回弹，恢复用户拖到边界时的可感知反馈，避免视口像卡死一样完全不动。
- 空间范围计算递归提取标准嵌套 Polygon 环坐标，合法面要素不再被误判为空。
- 同步更新空间分享和地图交互需求文档，并补充连续旋转缩放、边缘拖拽及标准 Polygon 回归测试。

## 1.5.35 - 2026-08-22

### 分享地图旋转边界修复

- 修复空间受限 KML 分享在旋转 90° 或任意角度后仍沿用归北状态拖动边界的问题，允许中心区域改为按当前旋转视口四角包络计算。
- 旋转、归北、缩放、窗口尺寸变化和惯性/键盘平移都会重新校正边界；窄视口下的超大非动画平移也会先按 bearing 转换中心，避免部分 KML 要素无法通过拖动查看或归北后交互状态混乱。
- 增加非正方形视口、直角旋转、任意角度和归北恢复的回归测试，并同步更新空间分享需求与地图交互文档。

## 1.5.34 - 2026-08-22

### 分享页面社交元信息

- 分享链接首个 HTML 响应使用分享设置中的标题和说明生成 `<title>`、Description、Canonical、Open Graph、Twitter Card、`itemprop` 和 JSON-LD，社交软件无需等待前端脚本即可识别分享内容。
- 预览地址统一移除密码、坐标和图层查询参数，继续保持分享页 `noindex, nofollow`；密码、Token、所有者信息和内部 KML 字段不进入元信息。
- 浏览器加载公开 manifest 后同步更新页面元信息，首屏服务端渲染和 2D/3D 客户端路由保持一致。

## 1.5.33 - 2026-08-22

### 分享低缩放底图管理员上限

- 移除“范围外底图放宽级别不得高于当前分享 `minZoom`”限制；单个分享值现在只受管理员配置的 `spatialUnrestrictedTileMaxZoom` 上限约束，默认值为 `14`。
- 用户分享对话框和管理员用户体系设置页使用同一上限，服务端预检、创建、更新和瓦片判定保持一致；管理员下调上限时存量分享自动收敛。
- 同步更新空间分享需求、API 契约、用户指南和回归测试。

## 1.5.32 - 2026-08-22

### 分享低缩放底图范围修正

- 补强空间范围公开校验，拒绝非法 `minZoom` 和高于 `minZoom` 的 `unrestrictedTileMaxZoom`，避免异常持久化配置进入公开清单或瓦片判定。
- 低缩放放宽阈值的接口、前端预检、用户指南和需求示例保持一致，并补充回归测试。
- 修正 MapLibre 构建兼容处理，仅替换已知 shader 片段，避免无关行尾空格导致构建资产不稳定。

## 1.5.31 - 2026-08-22

### 分享低缩放底图范围优化

- 空间受限 KML 分享新增可选 `unrestrictedTileMaxZoom`（基础为 `0～24` 整数且不得高于当前分享 `minZoom`）：配置级别及更小缩放允许范围外周边底图通过受控接口回源，更高缩放仍严格限制在 KML 外包矩形内。
- 前端相机最低缩放随该设置放宽，但 `maxBounds` 仍固定为 KML 外包矩形，用户不能拖动到不可视区域；低缩放直连瓦片不执行边界遮罩，仍经过分享鉴权、图源校验、限流并使用私有不缓存响应。
- 空间范围公开模型固定为 `version=2`、`geometryType=BoundingBox`；当前没有正式用户，旧测试分享不做兼容迁移，需删除后按新规则重建。无密码分享的 `passwordAccess.ttlMode` 固定为 `not_applicable`。
- 同步更新空间分享 API、需求文档和自动化测试；未设置阈值时保持原严格瓦片策略。

## 1.5.30 - 2026-08-22

### 分享链接视图状态恢复

- 分享 KML 链接刷新时优先恢复 URL 中合法的 `coords` 中心、缩放和旋转参数；只有不含合法 `coords` 时才自动适配完整的默认可见 KML，避免覆盖用户保存的初始视图。
- 2D/3D 分享页和路由切换保留 `coords`、`layer` 参数；底图按链接参数优先恢复，非法或超出空间受限范围的视图安全回退到允许范围。
- 空间受限分享继续使用固定 `version=2`、`geometryType=BoundingBox` 外包矩形模型；当前无旧版本兼容目标，旧测试分享需删除并按新规则重建。无密码分享的 `passwordAccess.ttlMode` 固定为 `not_applicable`。
- 补齐 API、需求和部署文档中的空间范围字段及管理员关闭无密码分享后的保存、同步、轮换拒绝规则。

## 1.5.29 - 2026-08-22

### KML 分享范围与密码策略修正

- 空间受限分享的权威范围改为全部 KML 坐标形成的轴对齐外包矩形，四边增加管理员余量；矩形内部全部可查看，不再按点位周边或几何缓冲联合区域授权。
- 新范围公开模型固定为 `version=2`、`geometryType=BoundingBox`；当前没有正式用户，不保留旧空间范围兼容分支，旧测试分享需删除后重新创建。
- 空间受限分享的边界瓦片改为按外包矩形执行服务端像素遮罩：保留矩形内底图，范围外像素透明，修复首屏整片绿色或空白；瓦片判定和遮罩使用同一矩形事实来源。
- 分享密码生成默认 12 位，提供 8/12/16/20/24/32 位选项和特殊字符开关；特殊字符限定为 `!$*+@`，排除 URL 查询分隔符，并继续对链接参数执行编码。
- 新增管理员控制的无密码分享开关：开启后可创建或移除密码，也允许 `expiresAt=null` 的无密码链接，其 `passwordAccess.ttlMode` 为 `not_applicable`；关闭后服务端拒绝新建、移除密码，以及继续保存、同步或轮换无密码分享。
- 同步更新 API、需求、用户指南和部署文档，明确旧测试分享删除重建以及无密码分享的管理边界。

## 1.5.28 - 2026-08-21

### KML 分享底图与密码交付修复

- 空间受限分享的边界瓦片改为服务端像素遮罩：保留 KML 联合范围内底图，范围外像素透明，修复首屏整片绿色或空白；线段穿越瓦片的空间相交判定同步修正。
- 遮罩处理限制为两个并发任务、24 个等待请求、2 MiB 输入和 24 MiB 字节 LRU；超限、解码失败或处理异常时保守返回合法透明 PNG，不放行未遮罩的边界瓦片。
- 密码分享以安全哈希继续校验访问，同时使用稳定服务端密钥保存所有者专用加密副本；复制带密码链接不再要求重新输入密码，并新增单独复制密码。
- 重构创建和更换分享密码对话框，统一标题、关闭、密码生成/复制工具和底部操作区的用户中心视觉风格。
- 用户数据库升级至 schema v8；同步更新 API、需求、用户手册和部署密钥维护说明。

## 1.5.27 - 2026-08-21

### KML 分享交付、访问统计与瓦片限流

- 密码分享新增普通链接与带密码链接两种复制方式；分享页自动验证查询参数并立即清理地址栏，修改密码后旧链接自然失效。创建和修改密码支持浏览器安全随机数生成高强度密码。
- 分享瓦片限流改为后台动态配置，默认提升至适合连续缩放和多图层加载的容量；只有图源与空间范围校验通过的瓦片消耗配额，范围外透明瓦片不再误触发 429。
- 公共分享使用稳定 HttpOnly 匿名访客标识降低 NAT 环境误伤；分享所有者可查看最近聚合访问时间、次数、方式、设备类型和来源 Origin，服务端不保存原始 IP、完整 UA 或 Referer。
- 管理后台新增全站访问统计脚本、分享托管 provider 和自定义脚本授权策略；分享所有者可按授权配置统计，管理员可逐分享禁用或恢复，客户端仅通过 DOM API 加载服务端校验后的 HTTPS descriptor。
- 更新 KML 空间分享用户手册，补充带密码链接、密码生成、访问记录、统计模式和限流排查；分享入口响应使用 `no-referrer`，生产日志仍需按部署规范隐藏首次查询参数。

## 1.5.26 - 2026-08-21

### 媒体预览紧凑状态栏

- 媒体预览底栏压缩为单行状态条：iframe 等普通资源约 30px，图片缩放资源约 34px；缩放按钮、比例文字和右侧元信息同步缩小且不换行。
- 标题区改为标题优先，类型、当前位置和集合名称依次后置；小窗标题栏收紧至约 30px，右上角操作图标视觉尺寸约 18px。

## 1.5.25 - 2026-08-20

### KML 批量管理与媒体窗口布局

- 2D/3D 个人图层管理面板新增跨文件批量选择、移动、复制和删除；操作经过统一校验，支持一次撤销并沿用现有账号同步。
- 媒体预览器在桌面宽屏上支持居中/宽屏切换，记忆用户偏好；小窗支持标题拖动、右下角调整大小和键盘方向键微调，位置与尺寸按视口安全边界持久化。
- 移动端继续保持全屏媒体预览，不显示桌面小窗手柄；媒体缩略轨道、资源释放和地图连续缩放逻辑不变。
- 批量选择工具增加当前可见要素的全选和反选；媒体 iframe 容器去除额外上下内边距并保持元信息右对齐。
- 小窗标题栏进一步压缩，新增左上角透明缩放热点；搜索面板增加快速关闭入口，地图菜单移除重复的管理后台按钮，修正左上角 Leaflet 控件 hover 边框裁切。
- 即使高德地图 SDK 未能加载，2D/3D 搜索面板仍保留可用的关闭入口；修正左上角透明缩放热点的方向光标。

## 1.5.24 - 2026-08-20

### 2D 触摸板缩放步长

- 恢复 2D Leaflet 滚轮/触摸板缩放的 `0.5` 级最小步长，避免 macOS 精细触摸板事件每次只产生约 `0.03` 级的不可感知变化；保留缩放动画、旧瓦片过渡和上一版本的方向/惯性隔离逻辑。

### 66 服务器资源保护

- 将生产 PM2 配置切换为 CommonJS `.cjs`，固定 `cwd`，关闭文件监听和应用自拉代码，移除每日强制重启；Node heap 上限为 640 MiB，RSS 达到 768 MiB 自动重启。
- 缓存统计改为流式目录遍历和有界最近条目，优先复用 sidecar 元数据，默认六小时冷却并在扫描期间保护事件循环，避免大缓存目录造成内存和 I/O 峰值；完整文件计数和字节统计保持准确。
- 发布脚本增加主机资源预检、PM2 环境安全保留、运行参数核验和安全回滚，防止重建进程时丢失管理员密码、Token secret 或代理配置。

## 1.5.23 - 2026-08-20

### 2D macOS 触摸板缩放方向

- 将触摸板捏合与普通双指滚动划分为独立输入手势；模式或方向切换时清除 Leaflet 旧累计量，避免同一 debounce 窗口内旧方向压过最新操作。
- 捏合结束后短暂隔离普通滚动惯性尾流，保护期结束即可开始新的双指滚动；连续操作不再出现放大、缩小随机反向。
- 2D 滚轮缩放取消半级强制吸附，保留按钮的半级步长及 Leaflet 缩放、瓦片淡入和标记过渡；跨级缩放期间旧瓦片继续提供完整画面兜底。
- 新增触摸板状态机、Leaflet 适配器和最终地图配置回归测试，并补充 macOS 触摸板缩放专项需求与浏览器验收记录。

## 1.5.22 - 2026-08-19

### KML 多设备编辑冲突合并

- 账号 KML 恢复草稿升级为 v2，快照保存不可变完整基线；普通标签页、多设备和 SidePanel 修改同一文件时，使用 `base / local / server` 三方合并，互不相交的文件字段、Feature、资源项和单边顺序变化可自动组合。
- `409 KML_REVISION_CONFLICT` 无真实冲突时使用服务器最新 revision 自动重试一次；二次冲突状态随草稿持久化，刷新后重新读取服务器并重新计算，避免无限重试或复用过期选择。
- 自动合并结果会同步替换 2D/3D 当前工作文件并立即重绘，后续编辑继续基于合并后的同一份数据，避免界面旧副本反向覆盖刚接收的服务器修改。
- 新增逐项冲突处理，显示原值、本地值和服务器值，只替换用户选择的冲突项；暂不处理继续保留草稿，处理期间服务器变化会拒绝旧选择。
- 回收站文件保留本地时执行 restore 后再 update，永久删除文件保留本地时改用新 ID create；创建响应丢失继续通过 `syncClientId` 对齐，默认 KML 最终保持账号内唯一。
- 默认文件切换批次优先创建或更新新的默认 KML，再更新旧默认文件；目标文件仍在回收站时先单独恢复、下一批再切换默认。永久删除文件保留为副本时，账号级默认选择同步映射到新 ID，避免默认保护错误或保存死循环。
- 新增纯函数和同步恢复回归测试，覆盖字段/要素/资源项/顺序、删除与修改、旧草稿降级、自动合并统计、重试状态、回收站、永久删除及默认文件约束。

## 1.5.21 - 2026-08-19

- 修复 SidePanel 侧栏中嵌入分区会话过期后，旧 CSRF Cookie 与普通会话混用导致 `/api/v1/kml/sync` 返回 403、点位新增或修改无法保存的问题。
- 服务端仅清理失效的嵌入 Cookie；前端对可重放的 JSON 写请求刷新认证上下文并最多重试一次，继续严格执行 CSRF、来源和权限校验，同时保留失败后的恢复草稿。

## 1.5.20 - 2026-08-19

- 修复 2D 新增地图标注保存后误调用要素定位逻辑的问题，保存前后地图中心和缩放级别保持不变。
- 2D/3D 高德搜索联想会根据当前视图中心所在城市优先返回附近结果，移动视图后自动刷新，同时保留全国跨城市搜索。

## 1.5.19 - 2026-08-18

- 桌面端媒体点位统一为单击直接打开大窗、长按打开操作弹层，并抑制同一手势的重复触发；移动端行为保持一致。
- 媒体大窗标题、导航、缩略轨道和操作按钮统一浅色透明状态，按下与焦点状态仅突出墨绿色边框；PC 外部打开入口改为仅显示图标。
- iframe 缩略项按已识别 provider 使用内置抖音或 720 云图标，未知页面保留通用页面图标，完整媒体数量和轨道访问不变。
- 3D 页面隐藏 Cesium 品牌图标与渲染质量面板，默认保持自动质量；地形状态精简并合并到经纬度/海拔信息，异常时保留重试入口。

## 1.5.18 - 2026-08-18

- 恢复 2D 地图的 Leaflet 缩放、瓦片生命周期和标记层过渡；修复 1.5.17 性能配置关闭全局动画后，缩放时旧画面提前清空、白屏等待瓦片和画面撕裂的问题。
- 缩放回归测试改为验证最终生效的地图配置，并将连续缩放约束写入项目协作规则、KML 性能需求和瓦片渲染技术文档，后续性能优化不得再以关闭过渡为手段。
- 恢复媒体预览缩略轨道的全部可预览项目，保留图片懒加载、离屏绘制跳过和关闭释放；修复大文件只剩首项、邻近项和末项共最多 7 项的问题。
- 移除点位名称 120 项永久上限，改为对全部可见名称执行可取消的空闲分批挂载；性能虚拟化不得再静默裁剪 KML 要素、名称、媒体或管理列表。
- Chrome `1280×720` 回归中，`177.kml` 的 110 个要素全部出现在管理列表，列表点击到点位弹窗约 112ms，46 个可预览资源全部进入缩略轨道且画廊构建约 2.3ms；合成 150 点位和 150 媒体场景分别保留 150 个名称与 150 个缩略项，连续滚轮缩放期间已加载瓦片数未降为 0。
- 修复资源集合编辑器地址字段被挤入操作列的问题，卡片/列表模式现在会真实改变编辑布局，移动端不再出现横向溢出。
- 批量地址输入在重绘和切换布局时保持内容，成功添加后清空并跳过重复地址；新增空白资源占位项不会再阻塞保存。
- 保存校验错误会定位到具体资源项和字段，自动切换分页、滚动并聚焦；编辑器存在未保存修改时关闭会进行确认。
- 不可预览的集合资源提供原地址入口；集合点位悬停仅改变阴影，不再缩放 Leaflet 定位节点，修复图标漂移和抽搐。

## 1.5.17 - 2026-08-18

- 针对 `177.kml` 及多文件场景优化 2D KML：媒体识别、坐标转换和图标 descriptor 缓存，popup 延迟生成，列表定位取消固定等待，分享只读 KML 也按视口增量挂载。
- 点位名称标签改为浏览器空闲后按视口挂载，缩放或平移期间主动释放标签 DOM；同坐标同名称标签自动去重并受总量预算约束。媒体画廊默认不展开资源集合内容，集合点位只按资源数量判断媒体交互能力。
- 普通 KML 点位缓冲范围收紧为当前视口 2 倍；分享页先完成范围定位再仅构建一次图层，折叠文件不再生成隐藏要素行，连续导入只展开当前文件，并跳过屏外列表行的布局与绘制。
- 新增 Point 资源集合：支持最多 300 项资源、列表/卡片分页、批量地址、统一大屏预览、2D/3D/KML 导入导出及分享只读浏览；关闭面板后释放媒体源和监听器。
- 资源集合打开时只识别当前 40 项窗口，进入大屏预览时才按需准备完整浏览序列，避免 300 项集合阻塞地图交互。
- KML 视口重渲染、标签空闲任务和媒体激活定时器统一绑定地图生命周期，地图卸载时主动取消；2D/3D 折叠卡不再计算文件详情或构建隐藏工具与要素行，展开时按需重建。
- 视口清理器在异步 KML 初始化开始时立即注册，地图实例提前销毁也不会遗留延迟任务；个人/公共分区收起时跳过所有子卡片渲染，重新展开再按需构建。
- 资源集合媒体预览的 `Escape` 事件只关闭最上层媒体预览，不会连带关闭集合面板；补充真实 Chrome 单文件、三文件和 300 项集合烟测记录。
- 补充资源集合解析前与规范化后的 512 KiB 双重上限测试，以及媒体切换/关闭时播放器、iframe、HLS、缩略观察器释放的回归契约。
- 集合 URL 增加 HTTPS、凭据和敏感查询参数校验；普通 720 云视图参数保留，历史公开快照读取时再次清理非法集合；非法或未知集合导入不阻断 KML，并通过 `warnings` 返回原因。
- 新增资源集合模型、媒体热路径、KML 往返、用户内容、公共 KML 和公开分享安全回归测试，并同步需求、API 和用户手册。

## 1.5.16 - 2026-08-17

### SidePanel 嵌入式 KML 编辑

- 新增 SidePanel 类 iframe 的分区登录会话：嵌入请求使用受控上下文头和 `Secure; SameSite=None; Partitioned` Cookie，普通标签页继续使用原有 `SameSite=Lax` 会话。
- 嵌入登录仅在页面来源仍与 map-service 完全一致时兼容跨站 Fetch Metadata，伪造嵌入标记的外部来源继续返回 `403 CSRF_INVALID`。
- 2D/3D 未登录 iframe 不再回退访客 KML；导入、新建和编辑入口保持隐藏，会话失效后先保存账号草稿再进入登录门禁。
- 小型账号 KML 恢复草稿同步保存完整 localStorage 副本，SidePanel 快速关闭或 IndexedDB 写入被中断时仍可恢复；大型草稿继续由 IndexedDB 保存完整内容。
- 修复恢复对话打开期间默认 KML 规范化误触发空同步、提前清理恢复草稿的问题，2D/3D 均先完成草稿决策再启动后续同步。
- 新增 SidePanel 双屏编辑需求、接口契约、用户手册及分区 Cookie、CSRF、来源校验和草稿恢复回归测试。

### KML 分享发布控制与地图交互性能

- 规划并开始实现个人 KML 编辑态与公开分享快照分离：KML 修改不再自动发布，分享所有者可显式同步内容并原子重算空间范围。
- 优化 KML 面板定位、标注保存后的视图保持、媒体预览点位名称和桌面 2D 连续缩放；性能治理同时覆盖常驻点位标签与高顶点线面渲染热点。

## 1.5.15 - 2026-08-17

### KML URL 与要素整理

- 720 云受控地址在严格 provider 校验后保留查询参数和片段，识别、保存、重新编辑及 iframe 重建不再丢失指定场景或视角；稳定 canonical 地址和现有安全边界保持不变。
- 2D、3D 编辑点位、线段和区域时可以选择其他个人 KML，并在保存时选择移动或复制；复制保留原要素并生成新 ID。
- KML 数据管理面板新增文件内拖拽排序和跨文件拖放，跨文件操作使用统一 Dialog 选择移动或复制；只读、公共、分享和部分过滤列表不启用拖拽。
- 要素操作复用现有数组顺序、撤销/重做和账号 KML revision 同步，新增纯函数与 provider 回归测试，并同步需求、API 和用户操作说明。

## 1.5.14 - 2026-08-17

### 地图定位、桌面旋转与 KML 品牌图标

- 修复 2D/3D 持续定位的相机所有权与异步时序：用户手势期间只保留最新定位目标，旧平移、飞行动画和迟到的单次定位结果不再覆盖当前视图；2D 自动旋转后定位点保持居中，停止定位后旧任务不再移动地图。
- 修复 2D 桌面端 `Shift + 左键拖拽` 松手后的视角规正跳动：预览改为围绕页面视觉中心补偿 pane 平移的完整合成变换，与 `leaflet-rotate` 最终 bearing 提交保持像素级一致。
- 旋转期间若发生缩放、程序性平移或容器尺寸变化，预览会按 transform 所有权安全让位给 Leaflet；已更新的平移或缩放不会被旧样式覆盖，尚未接管的临时旋转会被及时清除。
- 抖音视频与 720 云全景点位改用内嵌品牌 Logo，2D 点位、3D billboard 和 KML 列表复用同一图形定义，提升不同媒体内容的辨识度且不依赖平台图片热链。
- 增加定位相机、交互协调、品牌图标和桌面旋转回归测试，覆盖非零地图平移、缩放抢占、程序性平移及预览释放一致性。

## 1.5.13 - 2026-08-16

### KML 空间受限分享与半公开地图

- KML 分享新增“仅查看 KML 区域”模式，由服务端合并多 KML 点、线、面几何并计算带边界余量的权威范围；支持反经线、面积与对角线阈值、长轨迹首屏缩放和范围版本校验。
- 分享创建与编辑新增空间预检、范围摘要和密码授权模式；管理员允许时，合规空间分享可独立于全站访问密码公开，也可使用可撤销的不限期分享密码授权。
- 分享专用底图 catalog 仅返回受控公开栅格图源；瓦片请求逐次校验分享、图源、最低缩放和空间范围，范围外返回透明占位，所有分享瓦片统一使用 `private, no-store`。
- 2D 分享页限制相机边界、最低缩放和世界环绕，并关闭搜索、定位、路线、历史及 3D 等越界入口；空间受限分享访问 3D 时自动回到 2D。
- 管理后台新增空间能力、边界余量、总体阈值、不限授权阈值和策略影响预览；KML 或策略变化会自动重算、保守暂停不合规分享，并撤销失效的不限期会话。
- 增加分享清单和瓦片固定窗口限流、搜索引擎禁止索引、空间重算并发版本保护及公开响应脱敏；同步 API 文档、需求状态、用户操作说明和自动化回归测试。
- 新增管理员分享运行指标接口，聚合范围缓存命中、重算耗时与结果、范围外判定和限流事件，并返回脱敏的分享与授权会话快照。

## 1.5.12 - 2026-08-16

### KML 媒体预览小窗布局

- 小窗模式将媒体标题、当前位置、展开和关闭操作合并到唯一标题栏，移除遮挡媒体内容的重复恢复横栏。
- 左右切换按钮改用居中的 SVG 图标，并固定在媒体舞台垂直中线；桌面和移动端小窗保持一致。
- 新增小窗结构与居中规则回归测试，完整媒体预览、缩略轨道和关闭后再次打开行为保持不变。

## 1.5.11 - 2026-08-15

### 2D 旋转与移动端 KML 媒体交互

- 2D 桌面 `Shift + 左键拖拽` 改为合成层实时旋转预览，拖动过程中不再逐帧触发 Leaflet 全量 bearing 更新；松手后一次性提交最终角度并同步罗盘、URL 和本地视图状态。
- 触屏优先环境中的媒体 Point 单击直接打开统一媒体预览器，长按打开原点位简介；桌面鼠标、无媒体点位、线和面保持原有点击逻辑，2D、3D、个人、公共及分享 KML 行为一致；3D 长按识别时会提前抑制同一手势的点击事件，避免简介弹窗随后被媒体预览覆盖。
- 统一媒体预览器新增缩略轨道图标开关：移动端默认收起、桌面默认展开，单项媒体自动隐藏；触屏横屏同样使用全屏布局，收起后不再占用主媒体空间。
- 移动端媒体预览接入同 URL 历史哨兵，浏览器返回按钮或系统返回手势优先关闭预览；关闭按钮会回收自身历史记录，不留下额外返回步骤。
- 新增触屏环境、媒体轨道默认状态、历史哨兵、2D/3D 点位交互和桌面旋转回归测试，并同步需求与用户操作说明。

## 1.5.10 - 2026-08-14

### 搜索与桌面地图旋转体验

- 移除搜索结果后的“保存收藏”快捷反馈条，KML 点位中的图标收藏入口保持不变。
- 搜索历史改为持久化普通数字经纬度；历史项在 2D、3D 均可点击定位，旧的无坐标文本项自动隐藏，避免出现无响应记录。
- 修复 2D 桌面端 `Shift + 左键拖拽` 旋转卡顿：高频鼠标移动合并为每帧一次地图旋转，视图 URL 与本地记忆在手势结束后统一写入，不再随每个鼠标事件同步存储。
- 修正桌面拖拽方向：向右拖动表示向右转向，地图内容按符合观察习惯的逆时针方向旋转；移动端双指旋转和 `Shift + 滚轮` 保持原有独立能力。
- 构建阶段明确标注 Cesium 上游环境光着色器的兼容性依据；仅规范生成代码中的注释，不修改着色器运算和 3D 渲染行为。

## 1.5.9 - 2026-08-12

### 定位反馈体验

- 2D、3D 单次定位成功后不再生成包含“保存收藏”的整条快捷反馈栏，仅保留地图左下角已有的定位状态、精度和时间反馈。
- 搜索结果与 KML 点位原有收藏入口保持不变，持续定位、定位点展示和轨迹记录不受影响。

## 1.5.8 - 2026-08-12

### KML 720 云内容与可配置点位图标

- 支持在点位描述中直接识别 `https://720yun.com/vr/<ID>`、`/t/<ID>`（含 `www` 主机）公开地址，浏览器本地生成受控 iframe；不读取 720 云登录态、私有数据或任意网页内容。
- 抖音与 720 云使用不同的专属点位图标；普通图片、视频、音频和页面继续按媒体类型显示图标。
- 新增点位图标选择器，提供自动识别及观景、营地、餐饮、停车、相机等内置语义图标；2D、3D、管理列表、账号同步、公开分享和标准 KML 导入导出保持一致。
- 使用 `map-service:marker-icon` KML `ExtendedData` 扩展字段保存显式图标，后端仅接受固定枚举，未知值不会进入持久化数据。
- 已补充 provider、图标枚举、iframe 安全策略、KML 往返和前端交互回归测试，并完成 720 云示例页面的浏览器验证。
- 图标选择器改为单行横向快捷栏，末端固定“更多”入口；完整图标库按默认顺序在二级弹窗中展示，支持当前选中、最近使用（最多 5 个）和常用图标快捷排序。
- 扩充住所、饮水、卫生间、医疗、商店、充电、公交、火车、骑行、徒步、山峰和瀑布等内置图标；最近使用只在点位实际保存后写入本机，不同步到服务端。
- Point 点位名称改为非必填，空名称保存为 `""`，地图、popup、详情、媒体预览和 KML 管理列表不再显示“未命名点位”占位标题；无名点位仍保留无障碍定位名称。
- 修复图标快捷栏横向滚动带动整个标注属性弹窗横向滚动的问题；字段、工具栏和主弹窗现在建立明确的宽度与溢出边界，横向滚动仅保留在图标快捷栏内部。
- 精简标注属性辅助文案：移除点位名称的重复可选说明，图标与描述提示改为面向能力的短文案，不再枚举会持续扩展的平台和媒体类型。
- 在项目协作规范中新增界面文案克制规则，明确辅助说明的使用边界和通用能力文案的稳定表达方式。

### 两步路导入助手图标优化（0.3.7）

- 为 Chrome 扩展新增“轨迹双节点 + 导入箭头”专属图标，提供可维护的 SVG 源文件和 16/32/48/128px PNG，覆盖扩展列表、工具栏和选项页展示。
- 图标沿用 map-service 青绿色主色与橙色轨迹节点，不使用两步路官方商标或素材；选项页同步增加品牌图标，并补充 Manifest 资源完整性测试。
- 扩展版本升级至 `0.3.7`；已加载的开发版扩展需要在 `chrome://extensions/` 重新加载后才会显示新图标。

## 1.5.7 - 2026-08-11

### 两步路导入助手统计信息与 KML 文件详情（0.3.6）

- 从两步路公开分享页右侧可见信息中提取总里程、运动耗时和原作者；字段缺失时保留已确认字段，不猜测或填充空值。
- 将上述统计写入 KML `Document.description`，服务端清洗后只追加一次规范化来源链接；账号中心、2D/3D KML 管理面板、地图直接导入和各端 KML 导出均保留文档介绍。
- KML 管理面板展开文件时新增安全的“KML 详情”区块，展示文件介绍、要素总数和已加载的点/线/面统计；旧 KML 没有介绍时保持兼容。
- 扩展版本升级至 `0.3.6`，同步更新 API、需求文档和用户操作手册。

### 两步路导入助手多线段与点位名称修复（0.3.5）

- 修复分享页存在多条独立线段时只导入一条的问题：现在会合并页面运行态、接口回退和地图图层中的全部有效线段，并按坐标方向去重；原始 GPS/WGS84 线优先，地图图层仅补充缺失线段并继续执行坐标反算。
- 修复系统图层标题、序号和自动生成的“标注点 N”被写入点位名称的问题；仅保留原始标注记录明确提供的用户命名，未命名点位名称为空，描述和媒体资源仍保留。
- 两步路导入创建的 KML 默认使用简约主题，首屏只显示点位图标，不显示名称标签；用户可在 KML 管理中切换为常规主题。
- 扩展版本升级至 `0.3.5`，同步更新需求文档、API 说明和用户操作手册。

## 1.5.6 - 2026-08-11

### KML iframe 预览回归修复

- iframe 预览改为水平方向占满可用舞台、仅保留上下居中；移除固定 `1280px` 最大宽度和左右内边距造成的空隙。
- 抖音运行时预览地址使用 `%20` 编码的 `calc(100vh + 48px)` 高度参数，将官方白色底部跳转栏置于 iframe 可视区域之外，同时保留播放器主体和控制条。
- 点位 popup 媒体入口改为容器级事件委托，并跳过同一要素的首次重复激活；关闭预览后可直接再次打开同一媒体。
- 已完成 Vite 开发环境与 `http://127.0.0.1:3088/` 构建产物浏览器回归；`npm run check`、完整 `npm test`（433 项）、`npm run build` 和 `git diff --check` 均通过。

## 1.5.5 - 2026-08-11

### KML 点位第三方分享链接媒体

- 新增通用 KML 分享链接 provider 适配器，首期支持从整段抖音分享文案中识别 `v.douyin.com`、抖音视频详情页、`iesdouyin.com` 分享页和官方播放器地址，并统一生成 `open.douyin.com` 官方 iframe 播放器。
- 2D/3D 新增和编辑点位会在保存时自动转换受支持链接；机器生成的 iframe 标记在再次编辑时隐藏并幂等重建，删除源链接后媒体同步移除，解析失败仍保留原描述且不阻断保存。
- 新增鉴权接口 `POST /api/v1/kml/share-links/resolve`，只对固定抖音短链和官方重定向域名执行 HEAD 解析，包含 CSRF、RBAC、频率、数量、超时、DNS/SSRF、手工重定向和响应脱敏边界，不提供任意 URL 代理。
- 富媒体解析器内置精确抖音播放器信任规则，统一媒体预览器按 provider 策略设置 sandbox、播放权限和全屏能力，并使用规范化详情页作为外链兜底；个人 KML、公共 KML和分享查看共用同一内容模型。
- 优化 iframe 预览融合效果：内容区、壳层和 iframe 统一使用预览舞台深色表面，iframe 在最大展示尺寸内居中并保留圆角裁剪；抖音运行时预览地址增加受控 `100vw` / `100vh` 参数，消除移动布局默认 `324×672px` 尺寸造成的白色空白条带，KML 内持久化地址保持不变。
- 新增需求、API、共享解析器、受控服务端解析器、接口鉴权和前端幂等转换测试。
- 使用需求示例短链完成真实上游解析验证；`npm run check`、完整 `npm test`（433 项）、`npm run build` 和 `git diff --check` 均通过，构建仅保留既有大 chunk 体积提示。

### 收藏入口精简

- 移除二维和三维地图工具栏中的“收藏当前地图中心”图标入口，避免与点位收藏操作重复；KML 点位弹窗中的纯图标收藏按钮以及搜索、定位等位置收藏流程保持不变。
- 删除不再使用的地图中心候选生成与按钮绑定逻辑，并新增静态回归断言，防止工具栏入口被意外恢复或点位收藏能力被误删。

## 1.5.4 - 2026-08-10

### 本机代理 DNS 委托与生产 DNS 污染兼容

- 修复 `v1.5.3` 只兼容 Clash/Mihomo `198.18.0.0/15` Fake-IP、仍会被运营商或服务器系统 DNS 污染结果阻断的问题；66 服务器实测 `www.google.com` 被解析为错误公网 IPv4 与受限 IPv6 混合结果，导致 Google 图源继续返回 403。
- 当代理出口明确为 `localhost`、`127.0.0.0/8` 或 `::1` 上的 HTTP/HTTPS 代理时，公开域名改由受控本机代理解析，避免服务端先固定受污染地址；直连和远程代理继续执行完整 DNS/SSRF 校验与 IP 固定。
- 显式 localhost、内网、link-local、metadata、保留地址和内部域名仍在进入代理前拒绝；公开接口仍只能按受控图源 ID 请求，不能选择任意代理或提交任意回源 URL。

## 1.5.3 - 2026-08-10

### 本机代理与 Clash Fake-IP 兼容

- 修复启用 `127.0.0.1:7890` 等本机 HTTP/HTTPS 代理后 Google 瓦片大量返回 403 的问题：Clash/Mihomo Fake-IP 模式会把 Google 域名解析到 `198.18.0.0/15`，此前统一 SSRF 校验在请求进入代理前将该合成地址误判为普通保留地址。
- 现在仅当代理出口为明确的本机回环地址、目标域名的全部 DNS 结果均位于 Clash Fake-IP 网段时允许通过；直连、远程代理、混合 DNS、127/内网/link-local/metadata 等目标仍保持拒绝，不放宽任意 URL 或内网访问边界。
- 代理出口测试、图源测试和实际瓦片回源统一采用同一规则，并新增网络安全、Fetch Relay 与图源诊断回归测试；使用当前本机代理配置实测 Google 瓦片返回 JPEG 200。

### KML 点位收藏按钮精简

- KML 点位 popup 与详情面板中的“保存收藏”操作改为仅显示收藏图标，移除重复的文字描述并收紧按钮占用空间；继续保留 `aria-label` 和悬停标题，确保键盘与辅助技术仍能识别按钮用途。
- 新增内联收藏按钮回归测试，确保后续不会重新渲染可见文本或丢失无障碍名称。

## 1.5.2 - 2026-08-10

### KML 资源点位名称定位

- 修复二维地图中带图片、视频、音频、页面或链接资源图标的 KML 点位名称与图标重叠的问题；自定义资源图标现补齐 Leaflet 名称提示锚点，名称与普通长按新增点位一致显示在图标正上方。
- 新增资源图标名称锚点回归测试，防止后续调整图标尺寸或样式时再次丢失名称定位契约。

### 两步路授权浏览器助手（0.3.4）

- 新增二阶段导入完成确认：浏览器助手读取并还原 KML 后先等待，只有 map-service 站内 API 明确保存成功才激活原发起标签页并关闭受管临时页，避免读取成功但保存失败时误关页面。
- 新增两步路页面非阻塞结果卡片，覆盖读取中、等待保存、登录/验证、读取失败和保存失败；自动处理不可用时提供“返回 map-service”“关闭此页”和“收起提示”，不使用浏览器原生阻塞弹窗。
- 标签页会话使用不可预测 `importSessionId` 并保存在 `chrome.storage.session`，校验源标签页、两步路标签页、官方 HTTPS 来源和 30 分钟有效期；只关闭扩展创建或登记管理且未固定的标签页，不搜索或关闭用户自行打开的其他页面。
- 地图页和账号中心都在保存成功/失败后回传最终结果；地图端仍完成账号同步登记、图层渲染和范围适配，成功后自动回到原地图标签页。
- 扩展版本升级至 `0.3.4`，同步新增用户操作手册、协议文档、标签页归属/自动关闭/失败反馈回归测试与静态安全检查。
- 本轮质量门禁：两步路/账号/API 专项测试 69 项通过，完整 `npm test` 411 项通过，`npm run check` 和 `npm run build` 通过；构建仅保留既有大 chunk 体积提示。

### 两步路授权浏览器助手（0.3.3）

- 修复图片媒体模型：两步路同一标注的 `commnFileUrl` 作为唯一原图 URL，`centerUrl`/`fileUrl` 作为同一媒体项的 `thumbnailUrl`；列表和媒体轨道使用预览图，点击后使用大图，不再把预览图登记为第二个媒体资源。
- 兼容 `<a href="原图"><img src="预览图"></a>` 及两步路旧 KML 描述，通用 KML 内容解析会合并为一个图片项；普通文本链接和非图片链接保持原有独立条目语义。
- 扩展版本升级至 `0.3.3`。旧版本已经导入的 KML 不会自动迁移，需删除旧副本后使用新扩展重新导入。
- 新增媒体主图/缩略图、HTML 链接包裹图片和安全边界回归测试。
- 本轮质量门禁：两步路与 KML 媒体专项测试 52 项、完整 `npm test` 409 项、`npm run check`、`npm run build` 和 `git diff --check` 均通过；构建仅保留既有大 chunk 体积提示。

### 两步路授权浏览器助手（0.3.2）

- 修复轨迹线与标注点坐标不一致：两步路 `trackLngs` 是 GPS/WGS84 原始轨迹，地图折线图层是底图坐标；此前按坐标数量评分时错误选中地图折线，导入后又按 `coordCorrection` 转换一次，造成整条线段位移。现在原始轨迹数据固定优先，图层回退也先调用页面 `changeMapCoordByMapType` 还原 GPS 坐标。
- 修复同一图片标注把大图和缩略图都导入媒体库：`commnFileUrl` 作为大图主资源，`centerUrl`/`fileUrl` 仅在大图不可用时回退；视频封面只作为 poster，不再作为独立媒体重复展示。服务端直连兼容转换同步采用相同规则。
- 扩展升级至 `0.3.2`；已由旧版导入的错位轨迹或重复媒体不会自动重写，需要删除旧副本并使用新扩展重新导入。
- （历史记录）本轮回归门禁：`npm run check`、两步路专项测试（37 项）和 `npm test`（405 项）通过；`npm run build` 与 `git diff --check` 通过，构建仅保留既有大 chunk 体积提示。

### 两步路授权浏览器助手（0.3.1）

- 新增 Chrome Manifest V3 两步路导入助手第三版实现：以用户可见的两步路页面运行态为主，将已展示的轨迹/标注/公开媒体数据还原为标准 KML；不复制 Cookie、账号、Token 或验证码，也不尝试绕过 SafeLine/WAF。
- 扩展升级至 `0.3.1`：按页面实际运行态读取 `trackLngs` 和 `trackMarks[].pointMsg`，保留路线标题、轨迹海拔、标注文本、公开图片/音视频地址；高德图层标注坐标按页面自身逆转换函数还原到 GPS 坐标。
- 页面提取链路改为运行态优先：新增主世界被动 Resource Timing 观察器和可独立执行的 `MapServiceTwoBuluPageExport`，读取页面实际资源 URL、轨迹数组与 Leaflet 图层后在浏览器内生成 KML；不改写原生网络 API，也不再把“没有标准 KML”作为页面展示轨迹的默认结论。
- 账号中心“我的 KML”和 2D 地图 KML 面板仅在“已登录、具备 `kml.own.write`、扩展已安装并授权当前站点”同时满足时显示入口；扩展缺失、被禁用、未授权或协议不兼容时入口保持隐藏，普通文件导入不受影响。
- 新增浏览器助手专用站内导入接口，继续执行 Cookie 会话、CSRF、RBAC、两步路 URL 规范化、10 MiB 大小、KML/XXE/XSS、要素配额、用户范围幂等和脱敏审计校验；`sourceMode`、`completeness`、`warnings` 与显式 `partialPolicy` 已贯通前后端，服务端不接收任何两步路凭据。
- 原 `/api/v1/kml/import/2bulu` 服务端直连接口保留为兼容/未来官方 API provider 能力，网站不再直接调用。
- 页面脚本已在真实两步路示例分享页验证：在轨迹正常显示后可直接生成约 70 KiB、2408 个坐标/标注要素的 KML，且不重复请求两步路轨迹接口；该历史验证对应 `0.3.1`，当前实测需重新加载 `0.3.2` 扩展，并可先运行页面脚本独立确认 KML 生成。
- 本轮质量门禁已复核：`npm run check`、`npm test`（403 项）、`npm run build` 和 `git diff --check` 均通过；构建仅保留既有大 chunk 体积提示。

### 两步路公开轨迹导入

- 登录且具备 `kml.own.write` 的用户可从账号中心“我的 KML”和 2D 地图 KML 面板粘贴两步路公开分享链接，直接导入个人 KML；未登录或只读用户隐藏入口，服务端仍强制鉴权和 CSRF。
- 新增受控两步路适配器：仅允许官方 HTTPS 分享地址，执行 DNS/SSRF、重定向、超时、大小、内容和频率校验；支持标准 KML 和无需登录/验证码的公开轨迹坐标转换，部分轨迹必须由用户显式允许。
- 两步路公开标注媒体仅保留固定下载主机、固定路径和唯一公开资源定位参数，拒绝私网/任意主机、非标准端口及 Token、签名、验证码等额外查询参数，避免敏感链接进入分享和导出内容。
- 地图端导入成功后登记账号同步快照并自动缩放到轨迹范围，避免同步再次创建重复文件；上游要求登录、验证码、WAF 或私有加密时提示用户手工导出 KML，不保存或绕过第三方凭据与验证。
- 新增接口文档、需求文档和两步路适配器/权限/幂等/前端契约测试。
- 服务端直连兼容阶段曾完成专项质量门禁；该阶段现作为历史兼容能力保留，当前导入流程以“浏览器助手与运行态 KML 还原”（`0.3.2`）需求文档和协议为准，尚未执行生产发布脚本。

## 1.5.1 - 2026-08-07

### 生产部署兼容性

- 补全 npm 11.14.1 严格校验所需的可选 Peer 依赖锁文件元数据，修复生产环境执行 `npm ci --omit=dev` 时因 `@emnapi/core`、`@emnapi/runtime` 条目缺失而中止的问题。

## 1.5.0 - 2026-08-07

### 用户体系、RBAC 与多 KML 分享

- 调整匿名 KML 分享体验：分享密码改为独立的 4～128 位口令规则；2D/3D 分享页展示完整点、线、面要素列表并复用常规 KML 的定位、详情与媒体预览交互，仅禁用写操作；移除遮挡地图的分享横幅，首屏自动适配全部默认可见分享 KML 的联合几何范围。
- 完成 SQLite 用户数据库 v4、迁移、内置角色/权限、生产安全引导、密码哈希、可撤销会话、CSRF、多维登录/注册/密码验证限流、最近再验证和脱敏审计；v4 新增用户范围的同步删除墓碑，使按 `clientId` 发出的删除即使先于旧 create 到达，也能阻止文件被乱序请求重新创建。用户体系设置与对应审计记录使用同一事务原子提交。注册成功和用户名已存在统一受理，成功登录或密码验证不再清空共享 IP 密码喷洒桶；无有效超管时复用已有用户名会强制轮换为引导凭据、递增权限版本并撤销旧会话。
- 完成普通用户、管理员、超级管理员和自定义角色的服务端 RBAC；后台统一改用 Cookie + CSRF，会话不再向前端签发或保存在 `localStorage` 的管理 Bearer Token。
- 新增注册开关、用户创建/停用/重置密码/会话撤销、角色权限配置、用户体系安全设置、分享治理和审计日志后台页面。
- 个人 KML 完成服务端归属、配额、导入导出、版本冲突、回收站、批量增量同步和显式本地迁移；同步创建以 `(ownerId, clientId)` 幂等，独立账本保证永久删除后旧请求也不能复活数据，响应丢失恢复会按私有 `syncClientId` 合并为单一工作项；响应丢失后立即删除本地创建项时，会持久化 `deletedClientIds` 并由服务端按幂等键移入回收站或记录删除先行墓碑，不再在下次加载时复活。删除成功后客户端保留 `trashed` 快照，2D/3D 撤销会按服务端 ID 执行恢复；尚未创建成功的文件则先按 `clientId` 取消墓碑，再根据响应到达时的撤销/重做状态自动续发 create 或 `trash(clientId)`，恢复内容有变化时续发 `update`，避免界面假恢复；2D 删除现与 3D 一样先写入历史快照，可正常撤销和重做。登录态按独立读写权限进入只读或可编辑模式，不再自动读取或上传访客 localStorage KML。
- 新增按用户隔离的 KML 恢复草稿：完整载荷存入 IndexedDB，`localStorage` 仅保存轻量代次元数据和墓碑，并兼容旧版完整草稿；新代次完整写入中断时回退到最近完整草稿且不降低代次。草稿能表达普通快照删除、未知是否已提交的创建删除和精确 `pendingOperations` 在途批次；新批次发送前等待草稿落盘，响应未知时优先原样重放，明确 HTTP 失败时清除在途批次，首次落盘失败不会绕过恢复保护直接发送。恢复 pending trash/restore 时保留原快照并以草稿最新文件存在性为准，不会被服务器 active 清单覆盖请求期间的 undo/redo；处理其他冲突时丢弃已解决冲突项的旧 revision update，并继续保留其他非冲突在途操作。会话失效监听先于加载/恢复绑定；`409` 后暂停自动重试，可加载服务器冲突版本、另存本地冲突副本或保留草稿稍后处理，同时保留其他未冲突修改。
- 新增用户位置收藏，支持地图当前视图、搜索、定位和 KML Point 等来源创建、检索、编辑、删除及跨设备管理。
- 普通用户 KML 分享升级为独立分享包：一个稳定链接可包含 1～20 个 KML，支持顺序、默认显隐、视图配置、密码、有效期、下载开关、暂停、恢复、撤销、轮换和后台封禁治理。
- 新增匿名只读分享页；公开数据按分享项 ID 脱敏，分享底图仅开放受控公开栅格图源，瓦片请求不会通过任意已启用图源或 URL 绕过授权。
- 加固 KML 交互 DOM 属性转义、公开响应脱敏、异常错误脱敏、请求 IP 来源、IDOR、XXE、分享访问边界和登录/注册同源校验；图源、派生矢量资源和代理测试目标统一阻断内网/metadata/保留 IPv4 与 IPv6（含 `240.0.0.0/4`）、混合 DNS 和重定向绕过。直连通过固定 DNS lookup 连接已验证地址；代理模式把目标改写为已验证 IP，同时保留原始 HTTP `Host` 与 HTTPS SNI/证书主机名，避免代理再次解析原域名导致 DNS 重绑定，并补充相应自动化测试。
- 新增默认关闭的 `MAP_SERVICE_TRUST_PROXY`，认证限流和 Secure Cookie 统一使用 Express 在可信代理边界内解析的客户端 IP 与协议，不再直接信任请求转发头。
- 修复 Vite 开发代理重写 `Host` 后导致登录、注册和管理后台登录被同源来源校验误拒绝的问题；开发代理现在显式保留浏览器访问来源，不放宽服务端 CSRF 安全边界。
- 修复普通 Chrome 扩展或本机网络组件移除回环地址 `Origin` 非默认端口后，管理后台登录被误判为跨站请求的问题；登录和注册来源校验现在优先采用浏览器控制的 Fetch Metadata，允许 `same-origin`，继续拒绝 `same-site` / `cross-site`，缺失该信号时再严格校验 `Origin` / `Referer`。
- 通过 npm `overrides` 将 `brace-expansion`、`DOMPurify`、`protobufjs` 锁定到已修复版本，消除本次上线审计发现的生产依赖漏洞，同时避免无关的 Cesium 构建产物升级。
- Cesium 资源复制会先清理明确的生成目录再写入，防止依赖版本切换后把陈旧 Worker 分块带入 `service/app/`。
- 管理 JSON Store 对同一文件串行原子写入并使用唯一临时文件，图源目录合并并发初始化，避免高并发瓦片诊断日志触发临时文件重名和 `ENOENT`。
- 新增 [用户体系与多 KML 分享 API](./api-user-system.md) 及 [用户体系部署与运维](./user-system-deployment.md)，更新需求状态和主 API 认证说明。

### 用户体系已知运维边界

- 登录、注册和分享密码限流当前为单进程内存实现；多实例部署需接入共享限流存储。
- SQLite 当前以单机、单写进程为部署基线；回收站超期异步清理和超级管理员跨用户 KML 归属转移尚未实现。
- 数据库迁移前的自动备份尚未内置，升级必须先执行一致性备份流程。
- KML 导入暂不支持 KMZ、MultiGeometry 和完整样式体系；分享 scoped catalog 暂只支持受控公开栅格图源。

### 用户体系验收记录

- `npm run check` 通过，新增用户系统、后台、用户中心、收藏、KML 同步和分享模块均进入语法门禁。
- `npm test` 全量 362 项通过，覆盖认证、登录同源校验（含普通 Chrome 回环地址 `Origin` 丢失非默认端口的回归场景）、CSRF、RBAC、IDOR、KML 同步创建持久幂等性、数据库 v1/v2/v3 到 v4 迁移与部分迁移恢复、响应丢失后的 `syncClientId` 合并、在途批次持久化与落盘失败门禁、删除先行墓碑、删除/撤销/恢复/重删及响应未知期间的 undo/redo 状态矩阵、2D/3D 删除历史、恢复草稿代次/轻量本地元数据/最近完整草稿回退/冲突处理、AdminStore 并发原子写、图源直连与代理模式 DNS/IPv4/IPv6 SSRF 边界及双层 TLS 身份、收藏、分享生命周期、分享密码 4～128 位边界、分享只读浏览体验、公开脱敏、受控底图、XSS/XXE、安全引导会话失效、设置审计原子回滚、旧 Bearer Token 失效和前端契约。
- `npm run build` 通过并重新生成 `service/app/`；仅保留既有大 chunk 体积提示，不影响构建产物生成。
- Chrome 生产构建浏览器验收通过：受控双 KML 分享数据能自动适配默认可见内容范围，面板展示点、线、面列表，点位 popup 可进入图片预览和详情面板，分享模式不出现横幅、写操作或旧公共 KML 内容请求。
- `npm audit --omit=dev` 通过，当前生产依赖为 0 个已知漏洞。

## 1.4.57 - 2026-08-05

### KML 媒体适配与二维瓦片缩放修复

- 所有 KML 视频进入统一预览器后都优先尝试带声音自动播放；浏览器阻止带声音播放时静音重试，切换或关闭后立即暂停并释放旧视频。
- KML 图片大图预览以预览画布高宽为初始适配基准，小尺寸图片也会等比放大至可用区域，不再固定按原始像素尺寸显示；`1x` 和复位均回到完整可见的自适应状态。
- 修复二维 canvas 瓦片在缩放时被 Leaflet 按未完成图片误删的问题：只中止旧层级尚未完成的源图片请求，保留已绘制瓦片作为新层级加载期间的平滑缩放兜底，同时继续使用边缘像素保护带和无逐瓦片淡入方案避免黑色方格边框。

## 1.4.56 - 2026-08-04

### KML 媒体预览闭环与界面减扰

- 修复 `embed` / `object` 视频资源被误判为 iframe 页面、无法进入视频播放器的问题；支持通过 MIME、视频扩展名或 KML `MarkerStyleVideo` 提示识别无扩展名旧视频，并将真实大样例统计更新为 143 张图片和 7 个视频。
- `embed` / `object` 视频进入预览后自动播放；切换或关闭时自动暂停并释放媒体，普通 `<video>` 资源仍保持手动播放。
- 统一预览器每次切换媒体都会在地图层同步激活并打开所属点位 popup；关闭预览后地图保留最终浏览媒体对应的点位，形成媒体与空间位置的交互闭环。移除与实时激活能力重复的右下角点位跳转按钮，减少预览区域遮挡。
- 桌面全屏预览的顶部、底部控制层默认使用 0.3 透明度，悬停或键盘聚焦时恢复完整可读状态；移动端隐藏顶部/底部信息层，只保留主预览、切换、关闭和小窗等核心操作。
- 精简点位 popup：无真实名称时不显示标题，媒体速览卡片也不再显示“未命名点位”占位文案；无文字描述时不再显示“此点位包含可预览媒体”，媒体区移除“可浏览整个 KML”的重复提示；详情面板继续保留未命名点位兜底和完整信息。

## 1.4.55 - 2026-08-04

### KML 点位媒体首击预览与跨点位浏览套件

- KML 点位 popup 首次打开即展示受控图片缩略图和视频/音频/页面类型卡片；点击媒体不再必须先进入详情面板，详情面板回归为文字、坐标、图层来源和完整内容清单。
- 新增 `src/map/kml-media-gallery.js`，将当前 KML 内各点位的图片、视频、音频和白名单 iframe 聚合为稳定浏览集合；预览器支持跨点位/跨类型上一项、下一项、方向键、轨道定位和首尾循环。
- 统一媒体预览器增加媒体轨道、所属点位信息、全屏/浮动小窗切换和小窗状态的地图交互；切换/关闭时仍会暂停并释放视频、音频、iframe 与图片手势资源。
- 视频支持浏览器原生播放和按需加载 `hls.js` 的 HLS `.m3u8` 兜底，默认不自动播放；iframe 延续既有白名单、sandbox 和 `no-referrer` 策略。
- 详情面板视觉层级、媒体分组标题、图片卡片和 2D/3D 点位 popup 重新整理，移动端适配底部面板与安全区。
- 将 `temp/kmltest1.kml`、`temp/kmltest2.kml` 归档至 `tests/fixtures/kml/`，新增真实样例图库、跨点位索引和首击 popup 自动化测试；两份样例分别覆盖 11 项与 143 项图片资源。

## 1.4.54 - 2026-08-04

### 2D 地图缩放过渡与资源占用优化

- 修复边缘像素外延上线后，Leaflet 逐瓦片 200ms 淡入在卫星/道路双图层加载不同步时形成的缩放黑框；无缝瓦片加载后直接显示，同时保留 Leaflet 缩放期间的旧层级兜底和完整的层级裁剪时序，静止态及截图继续使用 258px canvas 的 1px 边缘保护带。
- 将 `keepBuffer` 从 10 收紧为 2，并移除瓦片画布不必要的 `willReadFrequently` 软件绘制提示，降低低性能设备上的逐帧透明度更新、canvas 纹理上传和内存压力。
- 补充瓦片层缩放性能配置回归测试，并扩展瓦片接缝技术文档，记录黑框与卡顿根因及优化边界。

## 1.4.53 - 2026-08-03

### 2D 地图瓦片无缝渲染

- 修复页面浏览和截图导出在缩放、旋转或长时间拖动后出现 1px 瓦片网格缝的问题。移除“负 margin 会自动扩大图片”的错误假设，改为中心 256px 内容加边缘像素保护带的 258px canvas 瓦片；截图离屏合成同步保留保护带和实际 margin。
- 增加瓦片外延纯函数测试与截图 canvas 瓦片回归测试。

### 工程会话载荷规范

- 记录 Codex 上游 `https://runanytime.hxi.me/v1/responses` 因请求体过大返回 413 的排查结论，并在 `AGENTS.md` 增加工具输出、日志筛选、截图传输和上下文恢复的载荷上限规范。

## 1.4.52 - 2026-08-03

### 彻底修复旋转地图截图的瓦片、KML 线段与点位错位

- 复盘 1.4.49—1.4.51 后确认，问题来自截图器直接重放 Leaflet Rotate 的嵌套 transform：父级旋转、瓦片自身平移和 SVG 局部坐标在克隆文档中被按不同参考系计算，导致瓦片缺失、KML 线段漂移或点位错位。
- 截图改为在当前页面中读取浏览器最终计算样式，按完整祖先仿射矩阵把已加载瓦片和 KML SVG 分别绘制到地图尺寸的离屏 canvas；点位、阴影、tooltip 和 popup 则按最终屏幕矩形克隆。截图克隆中隐藏原 Leaflet 图层，只输出这套统一的屏幕坐标快照。
- 修复 SVG 元素没有 `offsetLeft` / `offsetTop` 时矩阵变为 `NaN`、导致整层 KML 矢量消失的问题；截图前等待当前瓦片和图标加载，避免导出过程中出现临时空洞。
- `html2canvas` 改为 npm 本地依赖并随前端构建产物发布，不再运行时从 unpkg 注入脚本，消除离线、CSP 和第三方 CDN 故障导致截图不可用的风险。
- 新增截图实现回归测试，并将 `src/map/screenshot.js` 纳入 `npm run check`；浏览器验收覆盖未旋转、30° 和 -45°，确认瓦片、KML 线段与点位均与页面所见一致。

## 1.4.51 - 2026-08-01

### 修复旋转截图线段与瓦片/点位错位

- 修复 1.4.50 旋转截图时 KML 线段与地图瓦片、点位位置不一致的问题：线段虽已按屏幕位置精确渲染，但瓦片与点位仍按未旋转位置绘制。现同步把旋转 transform 下放到瓦片、点位与 tooltip 自身（html2canvas 对元素自身 transform 精确旋转），截图整体与屏幕所见一致。
- 通过旋转 30° 像素级验收：瓦片完整旋转、KML 线段位置与主文档一致、点位图标完整显示。

## 1.4.50 - 2026-08-01

### 修复旋转截图瓦片丢失与未命名点位面板展示

- 修复 1.4.49 引入的回归：截图改用 `foreignObjectRendering` 后虽修复旋转线段，但跨域瓦片无法内联导致底图空白。现改为截图时用 `getScreenCTM` 将 Leaflet 矢量 SVG 内容按屏幕变换预渲染为位图并替换为 canvas，瓦片保持默认渲染，旋转 30° 与未旋转下瓦片、KML 线段、点位均与屏幕一致。
- 修复 KML 面板列表中未命名点位名称显示空白：个人/公共图层列表与详情面板一致，以「未命名点位」兜底展示。

## 1.4.49 - 2026-08-01

### 修复旋转地图截图丢失 KML 线段

- 地图旋转（bearing ≠ 0）后使用 Alt+Shift+S 截图时，KML 线段/多边形会消失而点位图标正常：Leaflet 矢量要素渲染在单个 SVG 中，html2canvas 默认计算渲染模式会把 SVG 序列化为图片绘制，旋转后其宽高被替换为旋转包围盒尺寸导致内容错位丢失。
- 截图改用 `foreignObjectRendering` 由浏览器原生渲染 DOM 快照，旋转状态下线段完整保留；不支持 foreignObject 的浏览器自动回退到默认渲染模式。
- 通过旋转 30°/-45° 与未旋转的像素级浏览器验收确认修复。

### KML 未命名点位不再显示文字标签

- 导入 KML 时，未命名（缺失或空白 `<name>`）的点位不再被填充「未命名要素 N」默认名称，`name` 保留为空字符串。
- 2D Leaflet 与 3D Cesium 地图上，未命名点位只显示图标、不渲染文字标题区块，避免地图上出现大量无意义名称标识；点击后 popup 与详情面板以「未命名点位」兜底展示。
- 新建或编辑点位时清空名称，同样按未命名处理，不再回填「新增标注点」「未命名要素」等默认名称。
- 补充 `parseKML` 未命名点位保留空名称及空名称导出的自动化测试。

## 1.4.48 - 2026-07-28

### KML 统一媒体预览器

- 图片缩略图不再默认打开无样式的新页面，改为 2D/3D 共用的站内全屏预览器；图片首次展示完整居中，支持 `1x` 到 `6x` 的双指、滚轮、按钮和滑杆缩放，以及拖拽、双击缩放、复位、上一张/下一张和键盘切换。
- 预览器适配桌面、移动端安全区和横屏，固定标题、来源、关闭与缩放控件位置；打开时锁定页面滚动并管理焦点，关闭后恢复原触发按钮，不遗留图片变换或手势监听。
- 视频、音频和白名单 iframe 使用同一预览层及一致的原始资源兜底；视频保持稳定 `16:9`、`playsinline` 且不自动播放，音视频切换或关闭时停止播放并释放资源，iframe 继续执行既有 sandbox 与 referrer policy。
- 引入轻量 `@panzoom/panzoom` 作为跨桌面和移动端的图片平移缩放内核；普通链接继续通过带 `noopener noreferrer` 的明确外链打开，不尝试绕过目标站点限制。
- 为 `野兰谷.kml` 使用的旧图片下载地址增加固定白名单兼容通道，解决浏览器因源站证书域名不匹配而无法加载的问题；接口仅允许 `down-files.2bulu.com/f/dn1`，并校验访问控制、DNS 公网地址、`image/*`、有效文件大小和 20 MB 上限，访问日志对目标 URL 脱敏。

### 文档与测试

- 更新 KML 富媒体需求、前端架构和开发验收清单，补充媒体类型过滤、图库循环、缩放边界及“图片不再直接新开页面”的自动化测试。
- 使用 `野兰谷.kml` 完成 2D/3D、桌面、移动竖屏和移动横屏浏览器验收，11 个图片点位图标正确显示，`1280×2275` 样例图片在详情和统一预览器中正常加载；同时覆盖图片、视频、音频与 iframe 的布局、切换、关闭和资源释放路径。

## 1.4.47 - 2026-07-27

### KML HTML 媒体渲染与点位类型图标

- KML `description` 现可安全识别 `img`、`picture/source`、`video/source`、`audio/source`、`iframe`、`embed`、`object` 和 `a` 标签；无文件扩展名的媒体 URL 按标签语义分类，图片、视频、音频、页面和链接使用受控组件展示，原始 HTML 不直接注入页面。
- 2D Leaflet 与 3D Cesium 的媒体点位使用一致的图片、视频、音频、页面/链接图标；导入时保留 `styleUrl` 媒体提示，用户无需先打开点位即可识别内容类型。
- 个人 KML 和公共 KML 共用解析与安全分类规则，继续限制 HTTPS、内网/本地主机和 iframe 白名单，并补充媒体标签、音频、共享导入、危险 URL 与 marker 图标自动化测试。
- 修复 Leaflet 默认点位图标将 Vite 资源 URL 与自动探测的 `imagePath` 重复拼接、导致 `marker-icon-2x.png` 加载失败的问题。

### 三维地图交互与真实地形

- 新增 3D 相机交互适配器：普通左键拖拽与单指拖动优先平移，`Shift + 左键` 或中键围绕地表锚点旋转/调整倾角；滚轮、触摸板和双指手势使用受限缩放，并在 pinch、转向、倾角和双指平移之间按阈值锁定，避免一次手势中途跳变。
- 无地表命中时的平移改为按相机高度、FOV 与 canvas CSS 尺寸换算，并增加 CSS 位移及世界坐标位移双重单帧限幅；窗口级 pointermove 保证拖出 canvas 后仍可连续处理。相机、KML 与辅助线取点统一优先深度命中，再地形射线，最后椭球体，避免山地编辑落回椭球面。
- 三维模式首次进入时自动尝试加载受控真实地形；默认 provider 为 ArcGIS Terrain3D 的 PoC 接入，不自建 DEM。新增 `terrain-runtime` 纯状态模型：`standby`、`loading`、`verifying`、`active`、`degraded`、`fallback`、`disabled`；20 秒加载超时回退、固定 LOD 10 的双区域局部起伏验证、12 秒验证 watchdog、连续 3 次瓦片错误回退。临时 fallback 在 3D 档位按 1.5 秒、3 秒最多真正重试两次，离开 3D 不消耗未执行预算，手动重试会重置预算。
- 新增可见四段质量控件、按相机高度连续衰减的高程夸张、地球光照/大气、深度关系以及可选阴影/后处理；未验证 terrain 保持 1.0x，默认 `auto` 当前保守映射为 `balanced`（低空上限 1.20x），动态帧率自适应仍为后续工作。平面降级会明确提示实际按节能档执行。
- 山地演示改为围绕真实地表锚点的安全侧向视距，避免绝对海拔镜头进入夸张后的珠峰；相机净空同步按夸张后的高程计算。模式切换、平面化、山地演示、全局/方向复位均尊重 `prefers-reduced-motion`；Cesium 与地形服务 attribution 保持可见。
- canvas 聚焦时支持方向键平移与 `+`/`-` 缩放；`VITE_MAP3D_CAMERA_PROFILE=compatibility` 可独立关闭增强绕点/倾角并保留稳定平移/缩放，不影响地形、KML 或辅助线。

### 文档与测试

- 新增 [3D 相机交互与真实地形立体渲染 v2](requirements/3d-camera-interaction-and-terrain-rendering-v2.md)，取代旧专项中的交互/地形部分；明确 ArcGIS Terrain3D 是当前默认 PoC，Cesium World Terrain、MapTiler 与自托管方案均须独立完成授权、网络、凭据和安全审查后才能用于生产。
- 补充相机手势/键盘、质量控件 DOM/ARIA、场景质量、特性档位、低动效、地形运行状态/重试预算和 KML/辅助线地形射线取点测试，并更新三维架构、部署配置和浏览器验收说明。

### 定位坐标一致性

- 修复普通定位相对持续定位出现约 500—600 米固定偏移：普通定位此前优先使用高德定位并无条件把结果认作 GCJ-02，持续定位则使用浏览器 WGS-84 后由项目转换，两条 provider/坐标契约在高德转换失败或返回粗略位置时会分叉。
- 普通定位、持续 watcher 和恢复轮询现统一使用浏览器 WGS-84 坐标，并在渲染高德底图前由项目代码恰好转换一次为 GCJ-02；浏览器失败时不再静默采用坐标基准不明的高德/IP 结果。
- 路线规划的“我的位置”入口同步执行 WGS-84 到 GCJ-02 的归一化；高德定位实例禁用 IP 粗定位，并补充单次/watch/poll 坐标一致性及 provider 隔离测试。

## 1.4.46 - 2026-07-12

### 优化轨迹视口重渲染性能，消除移动地图时的顿挫感

- **问题背景**：1.4.45 扩大预渲染范围至 3x 后，视口变化时重渲染的数据量增大，导致移动地图时出现明显顿挫感。
- **根因分析**：
  1. **无缓存机制**：每次 `moveend` 都执行完整的 removeKmlLayers + 重新创建所有图层，即使在已渲染的缓冲区域内也重复渲染。
  2. **3D 事件过于频繁**：使用 `camera.changed` 事件，该事件在相机移动过程中持续触发（而非移动结束后），导致 debounce 计时器不断重置，最终在移动过程中触发重渲染。
  3. **requestAnimationFrame 阻塞绘制**：重渲染在 rAF 回调中执行同步重活，阻塞了浏览器绘制下一帧。
  4. **面板 UI 不必要更新**：每次视口重渲染都调用 `updateKmlPanelUI`，触发 DOM 重建。
  5. **debounce 实际仍为 200ms**：上一版只改了注释未改代码值。
- **优化方案**：
  - **视口缓存跳过**：新增 `lastRenderedViewportBounds` / `lastRenderedCamLat3d` 等缓存变量，记录上次渲染时的视口范围。若当前视口仍在上次渲染的 3x 缓冲范围内且缩放级别变化不超过 1 级，直接跳过重渲染。大部分小幅平移不再触发任何工作。
  - **3D 改用 `camera.moveEnd`**：从 `camera.changed`（移动中持续触发）切换到 `camera.moveEnd`（移动结束后触发），与 2D 的 `moveend` 行为一致，彻底消除移动过程中的不必要回调。
  - **`setTimeout(0)` 替代 `requestAnimationFrame`**：让浏览器先完成当前帧绘制，再在下一个事件循环中执行重渲染，避免阻塞绘制导致掉帧。
  - **跳过面板 UI 更新**：视口触发的重渲染不再调用 `updateKmlPanelUI`，避免不必要的 DOM 操作。面板 UI 在其他交互路径（开关、编辑等）中仍正常更新。
  - **debounce 调整为 150ms**：配合缓存跳过机制，150ms 足以合并连续事件，且不会在缓存未命中时造成明显延迟。
- **效果**：小幅平移完全不触发渲染（缓存命中跳过）；大幅平移在移动停止后 150ms 内异步渲染，不阻塞绘制。

## 1.4.45 - 2026-07-12

### 优化轨迹视口动态渲染的预渲染范围和响应速度

- **优化背景**：用户反馈移动视口时能明显感知到点位和线的渲染过程，体验不够流畅。
- **优化方案**：
  - **扩大渲染缓冲区**：`VIEWPORT_BUFFER_RATIO` 从 1.5 提升到 3.0，即渲染范围为当前视口的 3 倍，预渲染视口外 1 倍的区域，用户在正常平移时不会触发新的渲染。
  - **缩短 debounce 时间**：KML 图层视口变化重渲染的 debounce 从 200ms 降低到 80ms，让重渲染更及时。
  - 两项优化配合，使用户在平移视口时几乎无感知：缓冲区足够大，平移后新进入视口的数据已在缓冲区内提前渲染好；即使触发重渲染，80ms 的延迟也几乎不可察觉。

## 1.4.44 - 2026-07-12

### 修复持续定位时历史记录只剩最近一次的问题

- **问题根因**：1.4.41 版本将视口过滤 + LOD 应用到了 `renderHistoryPoints`（直接 circleMarker 渲染），导致持续定位时 `map.setView` 将地图居中到当前位置后，之前的历史点全部落在视口外被过滤掉，用户只能看到 1 个点，误以为历史记录丢失。
- **修复方案**：
  - `renderHistoryPoints` / `renderHistoryPoints3d` 恢复为 `slice(-N)` 策略（上限 500），不再应用视口过滤和 LOD。
  - 视口过滤 + LOD 仅保留在 KML 图层（`renderKmlLayers`）中，因为 KML 图层包含大量 LineString 和 Point 要素，是性能瓶颈所在。
  - 移除了 `location.js` / `map3d/location.js` 中冗余的视口变化监听器（`scheduleViewportRerender2d/3d`），因为直接 circleMarker 不需要按视口重渲染。
  - KML 图层的视口动态重渲染由 `kml.js` / `map3d/kml.js` 自身的监听器独立处理（1.4.43 已修复）。
- **影响范围**：2D（Leaflet）和 3D（Cesium）均修复。

## 1.4.43 - 2026-07-12

### 彻底修复视口移动后轨迹不动态渲染

- **问题根因**：1.4.42 版本的修复未生效，因为视口变化监听器只在 `startIntervalLocation` 期间注册、`stopIntervalLocation` 时注销，且有 `intervalLocationState.active` 前置条件。查看已保存轨迹时（非活跃定位期间）**根本没有监听器**，所以平移/缩放后 KML 图层不会重渲染。
- **正确修复方案**：将视口变化监听器从定位模块移至 KML 模块自身：
  - `kml.js` 的 `initKmlSupport(map)` 中注册 `map.on('moveend zoomend', scheduleKmlViewportRerender)`，在页面初始化时即生效，不依赖定位生命周期。
  - `map3d/kml.js` 的 `initKmlSupport3d(viewer)` 中注册 `viewer.camera.changed.addEventListener(scheduleKmlViewportRerender3d)`。
  - 新增 `scheduleKmlViewportRerender` / `scheduleKmlViewportRerender3d` 函数，debounce 200ms + rAF，仅重渲染 `isLiveTrack && enabled` 的 KML 文件。
  - 从 `location.js` / `map3d/location.js` 的视口 handler 中移除冗余的 KML 重渲染调用，避免双重渲染。
  - 移除不再需要的 `rerenderTrackKml2d` / `rerenderTrackKml3d` 导出函数。

## 1.4.42 - 2026-07-12

### 修复视口移动后轨迹线和点不动态渲染

- **问题背景**：1.4.41 版本实现视口过滤 + LOD 后，视口变化时仅重渲染了直接历史定位点（circleMarker / billboard），未重渲染 KML 图层（轨迹 LineString 和点要素），导致平移/缩放后视口内的线和点需要刷新页面才出现。
- **修复方案**：
  - 在 `kml.js` / `map3d/kml.js` 中新增 `rerenderTrackKml2d` / `rerenderTrackKml3d` 轻量级函数，仅按当前视口重新过滤和渲染已有 KML features，不重新生成数据。
  - 在 `scheduleViewportRerender2d` / `scheduleViewportRerender3d` 的 rAF 回调中，除调用 `renderHistoryPoints` 外，同时调用 `rerenderTrackKml2d` / `rerenderTrackKml3d`，确保视口变化时 KML 图层（线 + 点）也按需重渲染。
  - 面板 UI 同步更新显示过滤后的要素数量。
- **影响范围**：2D（Leaflet）和 3D（Cesium）均修复。

## 1.4.41 - 2026-07-12

### 轨迹渲染视口过滤与 LOD 分级优化

- **问题背景**：持续定位轨迹渲染原采用固定截断策略（120 点 + 2000 线顶点），长途行驶 30 分钟即超限，旧轨迹点从地图消失，轨迹线被大幅抽稀失真。
- **优化方案**：将固定截断替换为视口过滤 + LOD 分级 + 硬上限三重控制：
  - **视口过滤**：只渲染当前可见范围（含 1.5x 缓冲区）内的轨迹点，视口外不创建图层。
  - **LOD 分级**：根据缩放级别动态调整渲染密度——低缩放（0-7）仅显示线轮廓不显示点；中缩放（8-12）稀疏点 + 中等线密度；高缩放（16+）全密度渲染。
  - **硬上限**：视口内最多渲染 500 个点 + 5000 个线顶点，防止 GPS 密集抖动时性能崩溃。
- **视口变化按需重渲染**：2D 监听 `moveend/zoomend`，3D 监听 `camera.changed`，200ms debounce + rAF 节流，平移和缩放时自动重渲染可见区域。
- **数据完整性**：存储层、KML 导出和面板列表的数据不受影响，导出仍包含全部记录。
- **向后兼容**：`getTrackDisplayFeatures` 不带视口参数时退化为旧逻辑。
- **测试覆盖**：新增 30 个自动化测试覆盖 LOD 配置、视口过滤、线顶点抽稀、硬上限和向后兼容性。
- **需求文档**：详见 `docs/requirements/track-rendering-viewport-lod.md`。

## 1.4.40 - 2026-07-12

### 修复搜索与路线规划输入提示失效

- **问题现象**：在 2D 和 3D 地图中输入搜索关键字或路线起终点时，高德 AutoComplete 联想提示完全无响应；路线规划、逆地理编码等依赖高德 REST API 的功能均受影响。
- **根因**：提交 `50a2037`（1.4.34）为防止地图 URL 中的坐标状态通过 Referer 泄露，在服务端中间件和 HTML meta 标签中同时设置了 `Referrer-Policy: no-referrer`。该策略导致浏览器对所有跨域请求（包括高德 `restapi.amap.com`）均不发送 Referer 头，而高德 JSAPI 的 `inputtips` 等接口依赖 Referer 做域名白名单校验，未收到 Referer 时返回 `INVALID_USER_DOMAIN`（infocode: 10006），直接拒绝服务。
- **修复方案**：将 `no-referrer` 调整为 `strict-origin`。`strict-origin` 仅发送 origin（协议+域名+端口），不发送完整路径和查询参数，既满足高德域名校验需求，又不会泄露 URL 中的坐标等敏感状态信息。
- **改动范围**：`service/index.js` 服务端中间件响应头、`index.html` / `3d.html` 的 `<meta name="referrer">` 标签，以及对应构建产物。

## 1.4.39 - 2026-07-11

### 状态面板透明度调优与定位标签去冗余

- **磨砂玻璃透明度透底优化**：调低了底栏与定位历史面板的黑色不透明度，`#footer-bar` 的背景透明度降至 `0.4`，弹出式表格面板降至 `0.55`。配合毛玻璃滤镜（`backdrop-filter: blur`），在保持白亮文字及精度信号高清可读的前提下，极大地透露并还原了地图底色内容。
- **清除追踪过程中的文字标签干扰**：在地图定位控制按钮和三维空间的主定位点图标（Cesium 3D Target Marker）中，均取消了追踪成功（`tracking` 状态）后的文字标识与气泡浮动标签（如 `持续定位中` / `当前位置` 等），完全交由页面左下角的状态条展示，规避视觉干扰；但仍在点击启动（`starting` 等状态）阶段完整保留 `正在定位...` 等关键唤醒词气泡。

## 1.4.38 - 2026-07-11

### 定位状态底栏与历史面板暗色微光重构

- **与右侧菜单对齐的暗色胶囊底栏**：将 `#footer-bar` 状态底栏由亮色改为黑色透明背景（`rgba(0, 0, 0, 0.6)`），并配以相同的 `1px solid rgba(255, 255, 255, 0.12)` 浅色微光边框及毛玻璃滤镜，与右侧浮动控制按钮在视觉质感上完美对称统一。
- **高对比度文字与信号高亮**：文字调整为亮白色 `rgba(255, 255, 255, 0.85)`，且 WiFi 精度图标在暗色底托下升级为高对比度的青亮色（绿色）、亮黄色和亮红色，极大改善了各种复杂遥感与暗色底图环境下的信息可读性。
- **定位历史面板同步暗色微光重构**：将 `#location-history-panel` 面板样式同步重构为黑色半透明 `rgba(0, 0, 0, 0.75)` 毛玻璃卡片，表格字体、分隔线及表头均经白亮化调整，保持全局设计语言的高度一致。

## 1.4.37 - 2026-07-11

### 定位底栏高亮清晰化与历史列表面板

- **定位状态底栏高反差优化**：对左下角 `#footer-bar` 添加了高颜值的半透毛玻璃背景与微阴影胶囊边框，即使在复杂的遥感底图与暗色图源下文字和信号图标也依然一目了然，并增加了悬停交互的缩放和边框高亮。
- **状态文本统一订正**：将持续追踪定位过程中的状态文字由 “正在持续定位” 统一定制更正为更为精准且口语化的 “持续定位中”，并同步更新了自动化测试断言。
- **历史定位数据展示面板**：点击底部定位状态栏可弹出半透毛玻璃设计的 `#location-history-panel` 浮动历史面板，以表格形式直观呈现定位时分秒时间、定位精度米数以及停留时间。
- **定位记录 100 次性能硬封顶**：新增的历史定位数据列表在内存中只保留最近 100 次的样本，保证大规模持续追踪时前端 UI 不发生任何卡顿；同时确保此性能限制只局限于该信息列表，真实的底图定位轨迹图层记录依旧保持无限追踪的机制，不受影响。

## 1.4.36 - 2026-07-11

### 底部定位状态栏与信号反馈

- **底部定位状态反馈**：在页面左下角原版本号一侧引入一个 Flex 排版的状态条 `#footer-bar`，支持在不阻碍底图的情况下展示简短、清晰的定位进度与定位状态（例如 `正在定位...`、`已定位` 等）。
- **WiFi 精度信号强度指示**：设计了矢量的 SVG WiFi 信号图标，并与定位精度 `accuracy`（米）动态映射。当精度小于 15 米时以绿色点亮全部 4 格信号；大于 50 米时以红色点亮 1 格信号，并在信号图标后紧跟定位精度的具体数值。
- **定位时间反馈**：状态栏精确展示最新成功定位时的时分秒（例如 `17:16:45`），极大增强了用户的实时交互反馈。

## 1.4.35 - 2026-07-11

### 定位坐标修复与弹窗详情

- **单击定位坐标对齐**：统一 `getBestPosition` 使用浏览器 Geolocation 定位源（WGS-84），并结合 `positionToGcj02` 恰好转换一次，修复了单次定位中高德 SDK 带来的固定偏差，使单击定位与持续定位的坐标基准完全一致。
- **定位主图标弹窗详情**：在 2D (Leaflet) 与 3D (Cesium) 中实现定位图标点击弹窗。弹窗展示经纬度 (GCJ-02)、原始经纬度 (WGS-84)、定位精度、定位源、时间等；若处于持续追踪模式，额外计算并展示停留时长与实时移动速度。

## 1.4.34 - 2026-07-11

### 持续定位与 KML 列表优化

- **定位精度限制**：限制持续定位记录点精度在 50 米以内，过滤精度较差的点位，提高轨迹质量。
- **KML 列表顺序调整**：新建、导入及轨迹录制生成的 KML 文件默认插到列表最前面（默认 KML 之后），使用户能够最先看到最新的轨迹记录。

## 1.4.33 - 2026-07-11

### 持续定位可靠性

- **修复长途高速移动后永久断更**：原 2D/3D 实现把相对最后一个已接受点超过 200 米的位置一律判为漂移。默认 15 秒间隔下，该阈值只允许约 48 km/h；高速行驶、出隧道或后台恢复后会持续拿旧锚点拒绝所有新位置，手动重启清空锚点后才恢复。现改为结合采样时间、定位精度和最高 500 km/h 的动态阈值，并通过两个相互一致的新样本安全重定基准。
- **持续采集状态机重构**：2D/3D 从并发 `setInterval + getCurrentPosition` 轮询迁移到共享的单一 `watchPosition` 控制器；新增 provider 静默 watchdog、临时错误指数退避、权限阻塞/恢复、生命周期重建、provider 时间戳乱序保护和 generation 迟到回调隔离。用户停止后，旧回调、权限变化和页面恢复事件均不得重新开启定位。
- **假健康自愈补强**：连续时间戳回退、重复缓存位置、系统时钟回拨以及地图消费者 Promise 永久悬挂均会被检测并自动重建；静止定位也会刷新漂移锚点，避免停留数分钟后单个远距离噪点被错误接受。
- **定位源超时与降级**：高德和浏览器单次定位增加应用层硬 deadline，防止 SDK 不回调造成 Promise 永久悬空；高德失败或无效坐标会降级浏览器定位，连续失败进入有限 cooldown，所有回调保留原始时间戳和错误码。
- **实际健康状态展示**：定位按钮以可见徽标、管理对话框以详细信息展示 `starting/tracking/stale/recovering/suspended/permission-blocked` 等实际阶段、最后成功时间、连续失败次数和自动恢复次数，不再只依据用户启动意图显示“运行中”。
- **长途资源保护**：地图历史点和实时 KML Point 渲染均限制为最近 120 个，长线显示抽稀到约 2000 个顶点，完整数据仍用于存储和导出；热调低历史点上限会立即裁剪；KML 检查点至少间隔 15 秒，轨迹 feature 使用稳定 ID 并清理旧索引。
- **轨迹暂停与持久化加固**：关闭再开启轨迹记录会在同一 KML 中创建独立 LineString 分段，不覆盖前段也不跨接停录区间；保存采用事务回滚和指数退避，活动 KML 被删除后自动重建，停止时最终落盘失败会明确提醒，完整停止后释放轨迹内存。
- **坐标与隐私保护**：西半球 Leaflet 包装经度在写 KML 前归一到 `[-180, 180]`；地图页面启用 `no-referrer`，访问日志对 `coords` 参数脱敏，高德原始错误对象不透传坐标或 cause。
- **输入与存储降级**：定位间隔严格限制为 1—60 秒整数；localStorage 读取/写入被禁用时不再阻断地图和持续定位初始化，KML 可降级为页面内存模式。
- **保活竞态修复**：2D/3D 共用幂等保活控制器，处理 Wake Lock 申请中停止、系统自动释放、前台重取和音视频资源清理，避免停止后迟到的 Wake Lock 继续保持屏幕常亮。
- **3D 运行时修复**：修复相机 URL 同步引用未定义 `layerNameMapping`，以及定位扩散波纹把颜色回调直接作为材质导致 Cesium 停止渲染的问题。

### 持续定位测试和文档

- 新增持续定位 24 小时虚拟长跑、500 km/h 移动、漂移重锚、未来/重复 timestamp、系统时钟回拨、消费者悬挂、静默 watcher、权限/错误恢复、生命周期风暴、用户停止、旧 generation、单飞降级、分段录制、热裁剪和隐私脱敏测试。
- 完成 Chromium 下的 2D/3D 真实页面验收：定位按钮可见展示 `starting/tracking/suspended/recovering/idle`，页面冻结期间不再消费位置，恢复后会重建定位源，watcher 异常可降级为单飞轮询；用户停止后再触发 `visibilitychange/pageshow/resume/online/focus` 和坐标变化也不会复活。Cesium 验收期间控制台无渲染错误。
- 新增 [持续定位长期运行可靠性需求](requirements/continuous-location-reliability.md)，明确 Web/PWA 后台能力是 best-effort：页面被操作系统冻结或杀死后不能承诺继续执行；恢复执行时必须自动检测和重建定位源。历史条目中的“百分之百”“彻底解决”仅保留为当时记录，不再作为能力保证。

## 1.4.8 - 2026-07-08

### 前端

- **新增“自动旋转地图（车头朝上导航模式）”选项**：
  - **航向算法实现**：集成大圆航向（Bearing/Heading）三角计算公式，根据前后两个轨迹点位的位移方向自动得出当前的前进航向。
  - **视口自适应对齐**：
    - **2D (Leaflet)**：自动计算得出地图目标旋转角，通过 `map.setBearing` 逆时针修正，使前进方向始终垂直排列朝上呈现在屏幕顶端。
    - **3D (Cesium)**：在定位移动更新时，自动换算当前弧度航向输入 `flyToLngLat` 中的相机 heading，实现视口平滑朝前进方向飞跃。
  - **自适应防抖保护**：两点间位移小于 2 米的噪声及静止微动不触发旋转，防止原地漂移产生视角的快速翻转和眩晕感。
  - **保留旋转视角**：关闭自动旋转或停止持续定位时，不强行重置为正北，而是保持当前的旋转角度以满足用户连贯的观察诉求（界面已有指南针按钮可手动一键归正）。

## 1.4.7 - 2026-07-08

### 前端

- **轨迹信息富集与界面文案精简优化**：
  - **精细化轨迹信息展示**：更新 KML 文件内的 LineString 轨迹线段描述（`description`），实时计算并展现**轨迹记录开始时间**、**最新结束时间**、**总记录时长（小时/分钟/秒数）**和**总记录点数**，赋予用户直观完整的运动统计数据。
  - **定位管理界面文案全面精炼**：
    - 定位对话框主标题“持续定位配置” -> **“定位配置”**。
    - 管理对话框主标题“持续定位管理” -> **“定位管理”**。
    - 下拉切换开关“是否记录轨迹到 KML 文件” -> **“记录轨迹”**。
    - 下拉切换开关“轨迹记录精简方式” -> **“记录方式”**。

## 1.4.6 - 2026-07-08

### 前端

- **持续自动定位交互与数据落盘机制升级**：
  - **实时 KML 数据存储**：舍弃了手动的“保存”轨迹按钮，转为全自动实时存盘。定位运行期间，位置坐标与停留微动计算将实时追加、修改至当前绑定的本地 KML 列表中，避免因程序异常或手机切后台挂死引起数据丢失。
  - **中途随时暂停/继续**：可在持续定位运行中随时长按“编辑”参数。关闭“轨迹记录”即为中途暂停，开启即可继续录入到当前定位会话关联的 KML 文件内。只有完全停止定位并下一次重新定位时，才会创建新的 KML。
  - **精简“仅保留路线”选项**：长程记录易造成点位实体过多带来渲染和内存负荷。引入“轨迹记录精简方式”选项：开启后仅在 KML 序列中保存和追加 LineString 线段，跳过离散的 Point 节点，极大地减轻负担。
  - **长按响应时间缩短**：持续定位长按呼出时间从原先的 2s 缩短至 1s，提升交互响应灵敏度。
  - **文案与操作精简**：“取消定位”重命名为“停止定位”；在停止时自动抓取并存盘最后一个位置节点。

## 1.4.5 - 2026-07-08

### 前端

- **全面优化防休眠机制（Wake Lock API + 极简视频播放降级）**：
  - **背景**：针对部分浏览器（如 Firefox）、iOS App 内部 WebView、或因安全上下文策略等不支持/限制调用 `navigator.wakeLock` 的设备，原生 Wake Lock API 可能会失效或抛出异常，从而无法成功阻止暗屏。
  - **解决**：在 2D 和 3D 持续定位防休眠中引入了**双轨并行降级保活策略**：
    - **主方案**：优先调用 W3C 标准的 `Screen Wake Lock API`。
    - **备用方案**：一旦 API 被浏览器禁用或调用失败，自动且无缝地降级为**动态后台播放 1x1 像素极简无声 Base64 MP4 视频机制**。
  - **效果**：双保险方案成功结合了官方节能常亮和媒体播放防休眠技术，实现了跨移动平台（Android/iOS/不同网页容器）百分之百防自动息屏/休眠常亮。

## 1.4.4 - 2026-07-08

### 前端

- **引入标准 Screen Wake Lock API 阻止屏幕变暗/自动休眠**：
  - **背景**：原先的静音 WAV 音频机制能够有效防止 JS 线程后台挂起挂起，但在多数移动端浏览器（Safari/Chrome）上，音频播放无法直接阻止屏幕暗屏或由于系统超时引起的自动锁定休眠。
  - **解决**：在 2D 和 3D 的持续定位逻辑中，集成了 W3C 标准的 `Screen Wake Lock API` (`navigator.wakeLock`)。
  - **流程**：在用户启动持续定位时自动申请屏幕 Wake Lock，关闭定位时自动释放。同时，监听 Visibility API（`visibilitychange`），当设备因为用户操作或重开应用回到前台/亮屏瞬间，自动补偿并重新获取已被系统释放的 Wake Lock。

## 1.4.3 - 2026-07-08

### 前端

- **轨迹 KML 坐标提取格式修复**：
  - 修复了定位保存的轨迹 KML 在地图上渲染不出点和折线的 Bug。
  - **原因**：2D 侧持续定位历史点（`historyPoints`）中的 `pt.latlng` 是以数组 `[lat, lng]` 形式存放，但在导出 KML 时，代码误用 `.lat` 和 `.lng` 字段读取，导致全部有效经纬度均被当作非数字（NaN）过滤丢弃，进而产生空几何 KML。
  - **解决**：重构了 2D 和 3D 轨迹保存的坐标提取机制，支持对 `Array`、`L.LatLng` 实例及 `{lat, lng}` 对象的自适应混合解析。现在，保存的 LineString 轨迹线和 Point 定位点能够以完全正确的 GCJ-02/WGS-84 投影渲染在 2D/3D 底图上。

## 1.4.2 - 2026-07-08

### 前端

- **KML 数据管理面板优化**：
  - **刷新逻辑修复**：修复保存 KML 轨迹后无法在管理面板中实时呈现新加文件的 bug（现在每当展开/打开 KML 控制面板时，均会即时执行 DOM 重绘，保障列表百分之百同步更新）。
  - **移动端滚动与防穿透**：修复移动端下 KML 面板无法正常滚动或穿透底图的体验缺陷（在 2D 地图引入 `L.DomEvent.disableScrollPropagation` 与 `disableClickPropagation` 彻底隔绝底层地图拖动；在 3D 地壳引入全局触摸与指针捕获拦截 `stopPropagation`。完美实现移动端上面板高溢出列表独立滚动且不穿透、不卡顿）。
  - **鲁棒性安全校验**：对保存时的坐标和有效数字做严格的防御性空指针校验，防止在无经纬度或数据缺损的情况下执行 KML 序列化崩溃。
- **漂移脏点过滤与定位重试**：新增定位异常点（脏数据）检测与过滤机制。当两次持续定位的坐标位移差异常偏大（如超过 200 米的突变偏差）时，系统会自动将该数据归为可能的脏点，并在 500ms 后立即发起一次定位重试；若重试点位仍然超出合理偏差范围，将自动丢弃该点并静候下一轮循环。
- **持续定位体验升级**：定位按钮长按触发时间优化缩短至 2s，操作灵敏度大幅提升。
- **轨迹点容量配置与记忆**：配置弹窗新增“记住最近点位数”字段（默认 0 记住所有点），且时间间隔、缩放级别和容量配置支持通过 `localStorage` 记忆持久化，下次开启自动套用历史设置。
- **微动停留智能过滤**：优化轨迹产生逻辑，当移动幅度小于 10 米时，不再无意义地追加新轨迹点（避免冗余交叠），并在当前点上持续累加停留时间，供后续回顾。
- **一键保存 KML 标注**：管理弹窗提供“保存”选项，用户可将本次持续定位的轨迹直接转换并保存为本地 KML 标注文件。保存时将智能生成轨迹线（LineString）和带有定位时间、精度、停留时长等丰富富媒体信息的离散点位（Point），并即时添加在当前地图上并录入“KML 数据管理”面板。
- **管理菜单文本精简**：重构并精简管理选项文本为“编辑”、“保存”、“取消定位”三按钮极简交互。
- **黑屏后台定位中断修复**：引入无声音频后台保活机制（静默循环播放 WAV 静音音频，保活 JS 线程），配合 Page Visibility API 亮屏/切回前台瞬间的位置即时补偿，彻底解决手机休眠或切后台导致定位线程挂起中断的问题。

### 文档

- 新增动态 Feature Layer 视口加载与服务端聚合需求文档，细化公共 KML 的 BBOX 视口查询和服务端点聚合方案。
- 新增 KML 点位富媒体内容展示需求文档，明确链接解析、内容库、点位绑定、iframe 安全和分阶段落地路线。
- 新增 KML 点位富媒体第二阶段外部内容库与资产库集成规划，比较 Directus、Payload、Strapi、ResourceSpace、Pimcore 等方案。

### 后端

- 新增公共 KML 点位富媒体内容接口，支持从点位描述中解析 HTTPS 图片、视频、iframe 白名单页面和普通链接，并对敏感查询参数脱敏。

### 前端

- KML 点位弹窗新增详情入口，2D 和 3D 共用点位内容详情面板，支持图片、视频、iframe 和链接分组展示。

### 测试

- 新增 KML 点位内容 URL 提取、分类、安全过滤、脱敏和公共 KML 点位内容视图测试。

## 1.3.7 - 2026-07-03

### 后端

- 完成矢量图源、图源预设库、密钥池、代理、缓存和对外发布能力的后端闭环。
- 基于需要 Key 的预置图源厂商自动初始化默认空密钥池，管理员基于预设创建图源时自动关联对应默认池。
- 需要 Key 的图源启用前必须绑定至少一个可用 Key，避免空密钥池图源误启用。

### 前端

- 完成图源管理后台的图源预设、密钥池、矢量图源配置和发布项对接。
- 密钥池支持展示官方申请或控制台入口，新增 Key 时按厂商默认密钥类型和注入参数预填。
- 前台 2D 地图支持 MapLibre 渲染 `vector-style` 图源，3D 地图对矢量图源使用栅格降级策略。

### 文档与测试

- 新增矢量图源渲染与密钥池需求文档、国内外图源调研报告，并更新 API 契约。
- 新增默认密钥池、预设图源创建、矢量资源重写、密钥轮询、缓存和对外发布路径测试。

## 1.3.6 - 2026-07-02

### 工程规范

- 扩展 `AGENTS.md`，补充前后端分离协作、接口契约、后端服务层、安全、测试、文档和进度记录规范。

### 后端

- 新增图源目录服务，默认初始化高德卫星、高德街道、Google 卫星、Google 卫星 HD、Google 街道及现有组合图层。
- 新增预置图源库模型和接口，内置国内外栅格、WMTS、ArcGIS、Google、MapTiler、Mapbox、Stadia、HERE、PMTiles、天气和专题图源模板，模板默认禁用。
- 新增密钥池模型和接口，支持多 Key 脱敏保存、轮询、优先级故障切换、随机和权重轮询策略，图源按需绑定密钥池后由服务端注入上游请求。
- 基于需要 Key 的预置图源厂商自动初始化默认空密钥池，内置官方申请/控制台入口，基于预设创建图源时自动关联对应默认池；需要 Key 的图源启用前必须至少存在一个可用 Key。
- 扩展图源模型，支持 `xyz-raster`、`wmts-raster`、`arcgis-raster`、`quadkey-raster`、`mvt`、`vector-style`、`vector-tilejson`、`pmtiles-vector`、`pmtiles-raster` 等类型。
- 新增矢量资源代理接口，支持 Style JSON、TileJSON、MVT、glyph、sprite、sprite@2x 和 PMTiles Range 请求。
- 矢量 Style JSON 和 TileJSON 会被服务端重写为受控 URL，密钥和上游地址不会暴露给前端；派生 MVT/glyph/sprite 通过短期服务端引用回源。
- 对外发布项新增矢量发布路径，支持发布 Style JSON、TileJSON、MVT、glyph、sprite 和 PMTiles，并复用图源密钥池、代理、缓存和诊断日志。
- 新增图源、图层、代理出口、代理池、对外发布项的后端 CRUD 接口。
- 新增前台 catalog 接口 `/api/v1/map/catalog` 和受控图源瓦片接口 `/api/v1/tiles/:sourceId/:z/:x/:y`。
- 新增代理池模型，支持图源按固定出口或代理池选择代理，并对代理密码做脱敏返回。
- 新增多发布项对外 API，支持发布单图源、组合图层内图源和专用图源，Token 只保存哈希并只在创建或重置时返回明文。
- 移除旧版单一 upstream 对外接口 `/api/v1/external/tile` 及 `/api/v1/admin/tile-api/logs`，对外服务统一收敛到发布项模型。
- 瓦片缓存元数据新增 `sourceId`、`layerId`、`publishId`，缓存统计新增按图源、图层和发布项聚合。
- 缓存元数据新增 `resourceType` 和 Range 字段，支持按矢量资源类型聚合；带 Key 的上游 URL 在缓存统计中脱敏，PMTiles Range 纳入缓存键避免错段复用。
- 图源缓存 TTL 和 stale TTL 已接入 FetchRelay 写入与 304 续期路径。
- 新增独立图源访问诊断日志，记录代理图源访问耗时、代理命中、缓存状态和错误信息，保留行数与对外发布日志分开控制。

### 文档与测试

- 新增并完善图源管理与图层配置重构需求文档。
- 新增矢量图源渲染、预置图源库和密钥池需求文档，并补齐后端 API 契约。
- 重写 `docs/api.md`，明确前台、后台、代理池、对外发布和已移除接口契约。
- 新增图源目录、预置图源、密钥池、矢量 URL 重写、对外矢量发布路径、代理池、对外发布、组合图层发布、缓存元数据聚合和 PMTiles Range 缓存键等 `node:test` 覆盖。

### 前端逻辑修复

- 图源管理后台新增图源预设库子页，可基于后端预设创建默认禁用的系统图源，并自动匹配厂商默认密钥池。
- 图源管理后台新增密钥池管理子页，支持多 Key 配置、启停、轮询策略、引用范围、官方申请入口、密钥池测试和单 Key 配置测试。
- 图源编辑页扩展为新图源模型，支持 `entry`、`secrets`、`rendering`、`license`、矢量类型、PMTiles、密钥池和 3D 降级图源配置。
- 对外发布项示例新增矢量图源 URL 和 MapLibre 接入示例，避免仍按 XYZ 栅格示例展示矢量发布项。
- 前台 2D 地图接入 MapLibre-Leaflet 桥接层，支持通过 catalog 中的 `styleUrl` 渲染 `kind=vector-style` 图源。
- 前台 3D 地图过滤不能直接作为 Cesium imagery 的矢量图源，并在配置了 `rendering.fallbackRasterSourceId` 时使用栅格降级。
- 修复图源管理页对外发布表单，补齐专用图源、代理覆盖和缓存覆盖配置。
- 修复组合图层对外发布示例，按后端 source tile 接口输出多图源叠加 URL。
- 修复诊断日志“查看所有日志”路径，新增全部发布日志接口并展示加载错误。
- 修复前台 2D/3D 图层目录加载，统一使用后端 catalog、图源元数据和受控 fallback。
- 修复代理/图源/发布测试结果字段映射，代理池结果按成员成功数展示。
- 修复图源/图层变更后的预缓存目录刷新，并在组合图层预缓存估算中展示分源明细。
- 移除设置页旧代理设置和按图层代理策略入口，代理统一收敛到代理管理、代理池和图源策略，普通 relay 不再支持 `useProxy` 隐式代理。
- 移除图源代理策略中的“继承系统默认”选项，图源默认始终直连，发布项仍通过不启用覆盖来沿用目标图源策略。
- 图源编辑改为配置瓦片倍率 scale，瓦片网格尺寸固定 256，避免管理员输入任意瓦片像素导致前台渲染异常。
- 图源缓存和发布项缓存覆盖表单改为按天、小时、分钟编辑，提交时再换算为接口毫秒字段。
- 图源、组合图层和对外发布项列表的启用状态支持直接点击切换，减少进入编辑页的重复操作。
- 管理后台通用操作提示改为右下角 toast 弹出，不再在页面内容流中占位导致布局下移。
- 诊断日志页拆分为“图源访问”和“对外 API”两个视图，避免两类日志混在一起排查。

## 1.2.0 - 2026-06-17

### 管理后台

- 后台改为导航式信息架构，按概览、缓存、预缓存、图层、设置拆分页面。
- 后台前端拆分为布局、状态、工具函数和独立面板模块，避免继续扩大巨石页面。
- 预缓存支持高德地点搜索，便于快速跳转到需要缓存的区域。
- 预缓存任务继续支持按图层创建，并在界面和文档中明确图层选择。
- 新增图层配置页，展示厂商、类型、缩放范围、URL 模板、子域和默认代理建议。
- 代理设置支持按图层启用，默认 Google 图层走代理，高德图层不走代理。
- 普通瓦片请求会根据 URL 识别图层，并应用图层级代理策略。

### PWA

- 新增 Web App Manifest、Service Worker、离线页和 PWA 图标。
- 应用支持安装到桌面或移动端主屏。
- Service Worker 只缓存应用壳和静态资源，明确绕过 `/api/`、`/log/` 和 `/.cache/`。

### 工程规范

- 新增 `AGENTS.md` 项目协作规则。
- 文档统一改为中文维护。
- 禁止业务代码直接调用浏览器原生 `alert`、`confirm`、`prompt`。
- 新增统一 Dialog 组件，并替换现有原生阻塞弹窗调用。
- 移除公开缓存管理接口 `/api/v1/cache/fetch-relay`，缓存查看和清理统一收口到鉴权后台。

## 1.1.0 - 2026-06-17

### 管理后台

- 新增第一版管理后台 MVP，访问入口为 `/?view=admin`。
- 地图页新增管理图标，点击后打开后台。
- 新增管理员登录，使用签名 Bearer Token 鉴权。
- 新增系统版本、运行信息、瓦片缓存、访问统计、代理设置和预缓存任务面板。
- 新增上游瓦片请求运行时代理设置。
- 新增有边界限制的瓦片预缓存任务，并持久化任务快照。
- 建立 `docs/requirements/` 需求文档目录。
- 增加后台认证、设置持久化、代理传递、访问日志解析和预缓存瓦片规划测试。

## 1.0.0 - 2026-06-17

### 发布

- API、缓存、前端和测试完成现代化改造后的第一个稳定版本。
- 本版本包含破坏性变更，不保留旧 `/api.v1` 接口面。

### API

- 将 `/api.v1` 替换为 `/api/v1`。
- 移除无关或测试接口：随机文件选择、Wallhaven 选择、GitLab webhook、静态资源/包搜索辅助接口和 `/login`。
- 新增 `GET /api/v1/health` 和根路径 `GET /health`。
- 新增 `GET /api/v1/routes` 和 `GET /api/v1/openapi.json`，用于轻量 API 发现。
- 新增缓存管理接口，后续在 1.2.0 收口到鉴权后台。

### 瓦片代理缓存

- 将永久读缓存替换为基于 TTL 的缓存元数据。
- 新增 `HIT`、`MISS`、`REVALIDATED`、`STALE` 和 `BYPASS` 缓存状态响应头。
- 支持基于 `ETag` 和 `Last-Modified` 的上游条件回源校验。
- 避免失败、过小或非瓦片类型的上游响应写入缓存。
- 使用临时文件和元数据旁路文件实现原子缓存写入。
- 将上游白名单收紧到精确 host 和瓦片路径。

### 前端

- 地图前端迁移到 Vite 8。
- 将 Leaflet 和 `@amap/amap-jsapi-loader` 改为 npm 依赖管理。
- 用模块导入和哈希构建产物替换 CDN 与手工时间戳脚本。
- 将地图代码拆分到 `src/` 下的聚焦模块。
- 移除重复的 `map.html` 和无关静态 HTML、文本、二进制资源。
- 移除浏览器端 PouchDB 瓦片缓存，让缓存新鲜度统一由后端控制。

### 依赖和测试

- 移除与已删除工具脚本绑定的依赖：Directus SDK、dayjs、ExcelJS、lowdb、Meilisearch、MiniSearch 和 p-queue。
- 移除旧离线任务脚本、JSON DB 和随机选择工具。
- 新增基于 Node 原生测试运行器的 `npm test`。
- 增加缓存新鲜度、stale 回退、失败响应拒绝缓存、缓存绕过和白名单规则测试。

## 2026-06-17 - 现代化基线

### 依赖管理

- 依赖管理从 Yarn 迁移到 npm。
- 移除 `yarn.lock`。
- 新增 `package-lock.json`。
- PM2 watch 配置改为跟踪 `package-lock.json`。
- Node.js 基线提升到 >= 22.13，npm 基线提升到 >= 10。

### 依赖升级

- 升级运行时依赖到当前稳定版本，包括 `express@5.2.1`、`axios@1.18.0`、`cron@4.4.0`、`glob@13.0.6` 等。
- 移除未使用的旧构建链依赖。
- 添加 npm overrides，约束历史传递依赖使用已审计的安全版本。

### 地图界面

- 高德 JSAPI 从 1.4.15 升级到 2.0。
- Leaflet 统一到 1.9.4。
- 移除未使用的 jQuery CDN。
- 围绕 `index.html` 统一地图入口。
- 优化搜索面板和浮动地图控件。

### 服务端兼容

- 用 Express 内置解析器替换 `body-parser`。
- 更新 CORS、glob、cron、rotating-file-stream 等 ESM 兼容写法。
- 修复 cron 模块异步加载。

### 验证

- `npm outdated --json` 无过期依赖。
- `npm audit --omit=dev --registry=https://registry.npmjs.org --json` 无生产依赖漏洞。
- 浏览器验证 `/` 能正常加载 Leaflet 1.9.4、高德 JSAPI 2.0、瓦片和标记点。
| 2026-08-24 | 新增交互运维闭环 helper：保留清理、聚合指标、日志脱敏及 SQLite manifest 备份恢复，并补充中文部署说明与测试。 | interaction |
## 2026-08-26 - KML 目录、排序与分享点位聚合

- 新增 KML 目录管理、文件归属/排序、目录批量显隐和跨页面统一契约需求文档。
- 约定历史文件进入“未分类”，分享按保存时展开目录快照，点位聚合默认关闭且只作用于公开分享页。
- 实施记录见 `docs/requirements/kml-directory-ordering-and-share-point-clustering.md`。

## 2026-08-27 - KML 回收站、懒加载、批量操作与聚合策略

- 新增个人 KML 回收站入口、恢复和永久删除密码二次校验；同步接口拒绝 `deletePermanent`，避免绕过复核误删。
- 完善目录/文件排序、目录批量显隐和受配置控制的批量下载；KML 要素批量操作限定在单个文件内。
- 分享隐藏文件仅保留 manifest 摘要，显示、展开、导出和批量选择时按需加载详情，并合并同文件并发请求。
- 分享级 `viewConfig.kmlPointClustering` 支持 Point 聚合；管理员配置统一为 `share.kmlClusterForceEnabled`、`share.kmlClusterMaxZoom`、`share.kmlClusterMinPoints`，默认强制聚合关闭。
- 增加 KML 大规模点位/线段性能基准脚本及专项测试，保持完整数据和连续缩放行为。
